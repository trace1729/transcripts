# 夜航船 · 44 · 夜航船 - 聊一聊新版本的 MCP 协议大变更
[视频链接](https://www.bilibili.com/video/BV14puB65EDB)

哈喽哈喽，午休了，出来遛弯了。今天讲一下MCP吧。因为0728的时候，cos code又对MCP进行了一波史诗级的更新，本来以为这玩意儿已经死了，没想到又死灰复燃了。前几天COCODE他更新了一波MCP的协议的大改吧。之前也做MCP服务相关的，最开始的时候是CCE，后面是君耀博，然后到上周在协议上面，最大的变更就是上周出的那个无状态的，相当于就是一个简单的HTTP请求。

可以讲一下为什么有MCP。我理解就是因为模型它的知识是固定的，训练好之后知识就是固定的了，然后需要在让它回答问题的时候，给它动态注入一些外界的知识数据，或者调用外界的一些东西。这就是模型它与外界进行交互的一种方式。但这其实并不是MCP出来的原因。原因是你刚刚说的那些，现有的function call是完全可以支持的。它主要是为了统一，它把function calling，其实MCP最后转到给模型的请求，也是to call那套。

那为什么有MCP？是因为在几年前刚接触模型的时候，如果听到很多人说模型能获取当前的时间或天气，很多人会认为模型是在服务端直接去调了一个天气或时间的接口。但其实模型本身并没有直接获取这些知识，它是做了一个function call的操作，这个function call可以是在本地。模型并不会自己去调用，它会吐出来一个叫tool call的操作，让你去帮它调用。比如要获取当前的天气，它会去get weather这样一个方法函数，然后告诉你需要get weather，你再把这个get weather匹配到本地的一个函数，去执行。

介绍完tool call机制之后，有了tool call，很多事情就能解决了，模型能很快地获取外部的知识和状态。但为什么还需要MCP呢？就是因为tool call本身，你写的tool和我写的tool之间是不能兼容的，而且你写的这些tool，我不能直接去调用你的。就是我觉得MCP它是为了统一标准。虽然OpenAI的tool call格式是一样的，但是我暴露的那个tool的能力，模型返回tool call之后，比如我是agent的client，我解析出来这个tool call，就要去调用外部的服务，但外部服务暴露的接口五花八门的。

外部服务可能通过RPC或者HTTP等不同的方式，如果我们自己去写的话，要去调用外部的一些服务，比如去调用谷歌doc或飞书的接口文档，可能得自己去看飞书的API文档，从头搂一遍，然后包装成tools，做映射转换。这样的话，每个人去对接这些API时，自己都要实现一遍，去做那一层协议转换。而且每个人都有自己的想法，转得都不一样。MCP的出现就是为了解决这个标准化的问题。

就是做不了通用的MCP就干这件事情，但是有了MCP相当于把这个协议统一了之后，我本地就不用那个client了，我只要用MCP的client就行了。对，就是把端点一配就可以了。比如说飞书它作为一个MCP的server端，然后它提供这些方法出去，MCP的server，然后你只要有一个MCP的协议的client，然后能发现这个MCP，然后就是能用这些MCP的一些写冷面。OK对。

然后最开始最开始两年前的第一版本的MCP的话，他是嗯可能大家就觉得比较难用，就是SCE协议。ACC协议的话，他就是说它有两根线，一根是就是在最开始，这就是前几版他会有初始化操作，会拿到C类似于session ID的东西，就是知道你我作为一个这个client，我连上你的MCP server之后，这是一个当前的一个绘画的一个session ID的一个概念。

然后SCE的方案就是他们刚开始想的是，我去作为一个client，我去跟MC那个服务端交互的时候，我可能是动态的，一直会你过去我我过来，你过去我过来可能就是非常频繁的交流。可能他们我觉得他们就设计了这样一个协议方案，就是说会保持一个ACC长连接，当我连上连连上这个server端的时候，然后还有一个当我要去给server去发，比如说我要调用，就最最简单的get view这个view操作的时候，我去发给server，通过post rpc的方式走HTTP那发个request给server的时候，因为最开始的时候我们不是连了一个streamable的client和server之间连了个streamable的管道嘛，然后然后server端处理了之后，把response通过那个streamable回来嗯，可能他最开始的时候就走这条路。

但这条路的话有很大的一个弊端，就是说我们在如果你在内网或者是本地环境的话，你你自己的streamable连接可能还是非常顺畅的，但是你要做一个server端，你提供大规模的streamable那个对外的HTTP服务的话，你首先你在弱网环境下，你ACC也很容易就断联了。还有一个就是刚刚我们说的那那个操作的话，你因为在当时的那些那些SDK server或者是java SDK，或者是啊TYPESCRIPT SDK啊，这些server端这些它都是单机的嗯，就是你你你会发现刚刚我们是有两个连接，第一个连接是我们先去，我们我们先MCP的话，其实他你可以先说一下大概的步骤。

就是会你首先会发起一个初始化操作，初始化操作拿到session ID，然后我们还会去做，首先你拿到session ID之后，你不知道这个嗯MCP server拥有哪些能力，所以说你要做一个tools list操作操作，它会返回我当前这个MCP server返回了拥有哪些能力。拥有了哪些能力之后，比如说我有一串，比如说GITHUB可能一串就是100个能力，我可能然后我把这100个能力，通过我们最开始我们说的最开始用户去转，就是在开荒拓荒阶段，用户是自己去转这个操作，就是把MCP的那些能力转成那个open AI的function CALL的那个schema的那个那个那个咳咳嗯那个结构，然后塞到塞给模型，就塞到我们的chat completion或者是ANTHROPIC的message唯一的接口里面。

然后然后给模型，然后模型让模型自己去选择，你需要啊根据上下文你去调那个选择，你要对你要去调哪个tool啊，嗯然后调呃调比如我调了某一个tool，我还是get view之后，然后又回来回来之后发诶MCP client，他发现诶我要调gateway的这个tools，然后他又转一下，转成刚刚我们说的那个streamable方那个那个那个那个协议，然后把这个get view，通过我们最开始那个发给那个MCP那个server端嗯。

去调我们的MCP server，然后因为我们client和server之间有一个长连接，然后server端处理了之后，他会通过另一条管道把它response发回来。这样的话就是它的控制和数据是两条分开的流，分开流。而且SSE它是在弱网环境下，是很容易断裂的。

然后这个就你作为你作为一个MCP server的提供方时候，你需要考虑分布式的问题。因为你的就是说一个简单的，比如说我们现在的，大概大家基本上服务都是跑在K8S上面的pod上面，你单pod的是无状态的。嗯，但你在设计过程中基本上单pod的是无状态，但是刚刚在刚刚那个那个方案情况下，你在初始化阶段，你因为拿了那个session id，所以说你要在单pod的这个把这个session id去和某一个pod的绑定是吧，分布式化。

但是但是你现在那个时间点对应的SDK，它的初始化代码里面，它并不是无状态的一个SDK，所以说你就要去改造非改造非常多的东西，然后把这个SDK变成一个无状态化的一个SDK，你需要改它的源码去做。

然后还有一个就是刚刚我们只是拿了session id了，你要去做view的时候，你会发现哎我做view的时候，因为我们最开始连的时候，我这个client连的是和streamable连接，连的是你的pod B，但是我发view请求的时候，我可能路由到pod C。你你pod C和pod A之间，你这个pod C并没有持有你那个streamable的连接，所以说你很难去把这个streamable返回回去。这时候你就要在服务端去做一些消息管，消息通知的一些处理，把pod A的消息发给哦，pod C收到的那个view消息，发给pod A，pod A来做一个啊响应响应处理，然后把最后得到的一个响应，从pod A的ACC连接弄回去。

就是他他做了很多，就是因为那个SDK已经把session id砍了，我我就是很不理解为什么之前要有这个session id，之前为什么要有？因为现在他把SDK砍了，都变成无状态的那种交互，HTTP的类似于那种，对啊，这样的话其实你一个请求过来，可以把它随意路由给任意一个pod，就这样的话，就是你的那个MCP server可以无状态的部署，全是无状态服务。

但是之前为什么在第一版的时候，要引入这个session id呢？当时这个session id有什么作用吗？我觉得还是当可能他在当时的设计过程中，你可以看到我们当时的协议，因为他是双线去通信，所以说你需要一个session id，你知道我发了这个post view请求，他把所对应的那个session id要从哪个线回来，有没有可能就是他当时这样的设计，导致他必须有一个session id来才能实现这个他当时因为有这样的协议的设计，所以说必须要有session id的概念去实现这个能力。

然后第二版的话就因为太难用了，然后就推出，马上就过了几个月就推出了第二版，就是Streaming HTTP。Streaming HTTP的话，他还是有session id，还是有session id的概念，我觉得这个解决了SSE的那种弱连接的问题，对弱连接的问题和断联的问题。Streaming HTTP的话，嗯其实在在就是前几步它其实和我们是一样的，它只是没有两根线的通讯，就是说你在做request to url的时候，直接去发个请求，然后回来一个to url，Response，就是你的请求和response在同一个连接上。

然后你在做出货的时候也是一样的，你请求过去，你请求过去之后，他会在响应header里面去告诉你，我返回的是SSE呃呃那个流式的响应，还是说是呃正常RPC的一个同步的一个请求响应，但是这些都是在同一个连接上。我觉得他这时候去放还是保留session id的原因，还是因为他要去兼容之前的一些东西。

因为这在第二版的协议，协议第二大版的情况下，他是支持第一版的兼容。因为他还有一个还有一个还有一个方法是get的，get session的一个类似于get ACC啊，get session的一个一个方法。这个方法的话，就是说他因为你在牛市响应过程中，我可以通过我的session id去get一个在某一个时间，如果你中断什么的，我可以把这个响应再get一次。我觉得还是在兼容之前的一些老老的协议的一些东西。

但是你会发现它第二版的优化，还是解决不了什么，解决不了刚刚我们服务端的一个分布状态的不说。因为你因为你是在最开始，他需要去做session的引力子操作。你做session盈利操作的话，你还是要去做啊，你要去打了破A去做一个初始化，拿到session id，然后你下一次可能打到poo b，你做做突破，然后你下次突破历史，然后你下一次可能打或者再去做突破。

然后前上周的话其实大家有趋势，其实我们之前也出过，出过，就是讲过就是CI。现在其实AGENT已经完全的盖过MCP了，所以说上周的话就是为了让不让，可能是为了不让MCP这么难用，就是还是大道至简。我觉得大道还是就说你过度的设计，还是嗯你还是得回归一些事物的本质。

你还有就是为什么之前MCP要做一些有状态的操作，因为MCP它不仅有突破，他还有PREME，还有就source，但是其实可能这些大家都用的这些，实际上都没啥用，过度设计了，过度设计了可能也也是过度设计，就实际上用的人非常少啊。对用的人最多的还是说，最多的还是说就是说这个MCP的，他的放心扩的一个超能力对。

所以说上周的话就是直接把c king案例干掉了，就是把MCP给变成了一个纯的HTTP的，无状态的请求。其实这样的话就我感觉可以就干很多嗯，就很方便的一些事情，就是可能MCP又要great game吗，这个还不好说。如果因为我感觉可能比之前要稍微好一点，就是你之前不是说他那个MCP和之前相比，它翻了三倍的流量嘛，嗯但其实相比于AGENT的的一个爆发式的一个增长，这MCP只是翻了三倍的流量，感觉还是三四倍吧，对，三四倍还是说明他是在下非常在下降的一个阶段，嗯嗯对。

就就目前看CI AI的能力，因为AH呢它是跑在一个long time上面的，其实我更CI天生就长在被市里，对对我更其实我更看好CI。其实MCD的话，它更多的是说嗯，做对于一些就是现在A现在agent的有两种跑法，一种是跑在一个有装time环境的一个跑法，MCP就是哦agent的就是你的agent的，比如说你像codex或者你的COCODE，你是跑在一个long time，就是你的电脑上，或者是你的云端的一个沙河，沙河里面都是它，但是它都是有一个运行时的long time。

还有一种的话agent的跑法，就是说你跑到你的代码里面，就是你在你在你的你的，你可能是一个服务端的代码，服务端代码，你在做agent的时候，你给这个agent的挂了非常多的tools，然后我觉得这个时候MCP的能力就出现，就就就凸显出来了。因为他不需要run time，就你的意思是类似于用拍SDK的那种方式，在一段代码里面集成这个倍镜的的能力，然后你去解析他的掉了MCP，然后然后再再用MCP client去掉出去，而不是CLY。你要是你要是有要用CLI的话，你必须要有一个BACH吗，必须要有一个run time，对对对。

还有一个就是其实不一定是派一个SA，就是你简单自己写了个路嗯，就是一个where two的loop，然后你在loop的入口挂了非常多的MCP的server端，然后server的一些凸啊是挂进去，然后你就可以把你的A键能跑起来。只是做这A键呢它是没有不需要它不能去嗯，在你的run time里去CD到某个命令行，去cat某个文件夹或者write1些文具，但他那些工具是固定的，CD工具对那些工具是固定的，但是这样的好处的话是。但是这样的好处的话是

企业在企业的流程里面，它是可控性的，就是相当于他在一个大的workflow里面的一小部分，是agent的。对对对对嗯。因为他这样的话，他能做的事情虽然是有限的，但是在企业的一些场景里面，他就是要需要他去做一些我非常可控并且有限的一些事情的一些节点，做一个小的，让小的一个agent的一个循环对OK讲的有点长行。那今天就这样，就这样吧，拜拜拜拜嗯。