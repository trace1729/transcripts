# LLM架构·214·[LLMArchitect]02Qwen3DensevsMoE，MoE等效宽度，attentionvsffn，RMSNorm
[视频链接](https://www.bilibili.com/video/BV1ty4TzSEpg)

亲爱的朋友们，大家中午好。今天我们继续回到大圆模型架构这个系列哈，这个系列是对我们之前personal chi g p p这样一个大圆模型训练的架构原理的一个丰富的一个补充哈。这期我们继续探索这个千问三三十BA3B，然后继续对比它和dance模型的一个dance和MOE的一个更多细节上的一个深入的一个对比和理解哈。

好，首先补充介绍一点哈，这个千问三是一个全家桶，它有不同的size，它有dance和MOE，它还分their model和这个普通的对话模型，它基本上已经成了一个学术哈，学术用的一个标配哈。就如果你不具备或者你这个机构不具备一个预训练的一个基础的话，在前文三它基本上是一一切做后训练的一个标配的一个开源的一个模型。

好，我们再简单回顾一下哈，就是这个所谓的这个32B的一个dance和这个A30B、A3B这样一个MOMOE模型架构上的一个区别。首先层数上dance的是64，MOE的话是48。然后d model这一块这里面哦补充介绍一下，d model是整个贯穿这个模型始终的一个embedding的一个dimension，就是每个layer的token的、每个layer的input的token embedding是这样一个d model，每个layer的output的每个token的embedding也是个demode。然后d model这一块dance是5120，然后MOE的话是2048，这个也是巨大的一个降低。

关于注意力这一块都是GQA哈，然后这个MOE的话是64个头，每个头是一百二、一百二十八尾，然后MOE的话是一共32个头、128位，然后head dimension是没有变的，但是投数降低了一半。上一期我们也也注意到了哈，这个head size不等于这个投数乘以这个head number哈，哦我们只需要把这个double kill从一个方阵变成一个一个非方阵就可以了。然后这个GQA这块地貌呃，这个dance的模型是64个头，KV的group或者KV的HS的组数是八组，64个QUER头，八组KV的he子，所以就是八个query头会共享一组key value。MOE的那个GREGQA呢也是类似的哈，我们这这个这个地方我就没放就不说了哈，哦我这个我整理了个表格，大家可以看那个表格哈，它是32个头四组啊，大概整体砍半了哈，所以他也就是说也是八个query头，八个query头共享一组key和value哈。

那这一期呢我们一个很大的一个重点，就是介绍这个所谓的中间尾，就是个intermediate medium size。dance的中间围是25600，所谓的中间围就是它是出现在FFN或者MOP或者是MOE里边的，因为MOE它每个expert也是一个FVN，也是一个两层的MLP就GGIT哦，就up get it done哈，上一期我们介绍也非常非常清楚了。dance的是25600，也就是五倍的DEMODEL，5120乘以这个5。MOE的他有一个他真正的每个MOE的expert、每个expert它的这个中间围是768，它是小于这个2048的哈，它不是一个skill哈，它不是一个up的一个过程哈，但是这个名字上也叫up。就是对于dance模型的FFN他是二五，它是5120映射到25600，他是这样一个情况。但是对于MOE的每个export，它是2048映射到768，他是更瘦的专家，更瘦来控来调控这个参数量。

那这里边这一期我们可能要着重介绍的，就是所谓的这个等效宽度。对于MOE的等效宽度该怎么去算呢？这个K呢是一个激活的一个赚钱的数量，比如八个哈，纤维三三十BA3B，它就是八个激活的export，然后每个export的中间围是768，所以他等于是8×768，8×768是6144，是三倍的d model，就是dance的FON是五倍的DEMODEL。

就是这个参数呢，其实在MOE里面，在千问三三十BA3B这个MOE里面是没有用到的。为什么呢？它这里边有两个参数哈，一个是decoder sps step和这个MOVE MMLP only layers，就是哪些层用FFN用标准的dense的FFN，哪些是用走MOE。他这两个参数配置下来之后，就是每一层都是MOE，就每一层里边这个除了attention之外，另外的FFN部分都是MOE的。这个具体来说这个decoder sports step，就是说就决定了解码器中MOE的步长和频率，就是你这个layer的index除以，就模除以这个decode sports step等于零的时候，这些层会用MMOE的MOP，也就是就是dense的MOMOP，也就是这里边那个参数配置之下，就所有的乘FFN部分都是MOE。

这里面有一个可能容易误会的一个地方，就是我们知道这个我们上期也着重介绍了这个MOE的一个计算部分，就是有一个gating的一个门控的一个网络，映射到所有的expert的一个球，一个系数就一个score哈，然后拿到这个比如这个top top8的一个激活，top8大的那个gating in score，我们选择这八个expert，那最终经过一系列的FFN的运算之后，这八个expert输出之后，我们是用这个gating的score哈，做softmax之后算出的一个概率进行加和求全，也就是这里边其实不是一个concat的过程，不是768concat都一起，而是概率加权，仍然是一个768维的一个向量。

那这里边就是说他怎么就变成了一个8×768的宽度呢，那这就是等效宽度的一个意思哈，它不是concat。这里边该怎么理解呢，就是为什么一个概率加权求和的一个过程，它等效了这个专家的数量乘以这个每个专家的一个维度呢。这里有两个维度哈，第一是flops计算的一个维度，另外矩阵rank的一个角度。

我们知道dense里面有是up gate down三个大的神经网络的一个矩阵乘法，它的flops的话大家可以这样算哈，d model乘以一个DFF，就是三乘以d model乘以DFF。对于MOE每个专家，每个被选中的专家各做一次Up gate down，合计K次一共八个，一共K个专家每次的宽度是M，也就是三乘以d model乘以K乘以M。如果把两者的这个flops对齐，因为我们最终实现的是一个计算量的一个等价，拉两者拉齐大家可以看到哈，就是DFF就等于K乘以M，这就是等效宽度的一个来源。

也就是说单个的MLP也就是一个expert的表达能力是被降低了，因为它维度变变变变瘦了嘛，用更多的experts来承载多样性。从这个rank的角度，就是加权求和也可以等价改写成concat，你把这些向量concat起来之后，再乘以一个暗块系数的一个矩阵，他的rank也是等价于这个就原始的这个DFF，这这种形式哈。

我们简单去去算一下，就假如说我们一共K个vector，我们来看对比一下这个加权求和和这个concat，他俩计算上的一个等价性哈。就是每个vector呢它是地位的，然后他们前面有个系数，有一个概率的一个系数，with sum的话就是每个系数乘以它对应的向量，在整体再K个加起来。如果转化成稀疏矩阵的话，就是我们构造这样一个稀疏稀疏的一个矩阵块，就是每个阿尔法乘以一个ID哈，这是一个等单位矩阵，是D乘以D的。我们把它concat起来，它就是一个D×K×D的一个一个大的矩阵，一个比较扁的一个矩阵，那这样的话这个A阿尔法乘以这样一个concat起来的，把他们concat起来也是最终结果上是等价的。

大家可以用这样一个简单的一个一个numpy的一个代码来简单的去对比一下，这两者是一一致的哈，在技术上是一致的，也就是说其实你把他们加强求和。也就是说其实你把他们加强求和

和他们concat起来，他举证的rank的举重，举重量角度他是大概也是不变的，也就是他承承载了足够多的多样性。虽然最终只有798、768位，但是他是八个expert merge起来的，但这个merge是with the sum。好，我们再看一点，我们在最后整体对比一下这两者的更多的一个细节的一个参数。我们整体汇总一下，hidden size 2048对5120，然后hidden dimension是没有变的，128，然后layer四十八六十四，然后这个has是降低了很多，降低了1/3，对，降低了一半哈。然后group k v的group降低了一半，然后这个等效宽度，所谓的等效宽度，我们这个6144是三倍的d model，然后这个2525600是五倍的d model，vocab size没有变，是共享大家共享词表的，然后这个位置的编码是40K也没有变，我们看下面的其实都没有变化，然后就是多了这个MOE的部分。

128个expert，每个token的激活的这个expert是八个MOE的，中间围是768，也就是2048映射到768，然后gating然后up gate down3个model，那三个三个三个矩阵，这个就是控制decoder sports step就是控制多久我们要走一次dense的MLP。这是dmodel关于DMODEL，就是每个DMODEL是贯穿这个模型的始终的，就是每个layer的input的token的embedding是DMODEL，每个layer的output这个token的embedding维度也是DMODEL。DMODEL的一致性方便做残差，这个比较好理解，就是你变化变化变化后的和变换之前的加起来，他俩维度是一样的，是可以做加加和的。

然后另外一个事情，其实我们在看这个MOE的这些参数的时候，大家可以看到Per token，每个token激活了多少个专家。这里边我想讲一个事情，就是MOE或者是FFN，因为MOE本质上每个expert也是FFN，他是一个token level的，就我在这恒照之前发了一个动态，其实也讲过这个事情。就是这种是causal attention，causal mask的attention就是因果注意力，也就是说我们当前的query只可以当前的query只可以看到之前的K看不到之后的K，这是一个因果的。然后这是一个交互，他刻画的是一个token之间的交互，Interaction。但是如果对于MLP的话，它是一个token的projection，token之间是没有交互的，也就是说attention呢是刻画的是token之间的交互，但是他是因果的，是当前的query可以看到过去的K，当前的query看不到未来的key，这是一个有一个因果的一个自回归的一个过程。

FFNMOE的话，它是一个token的projection，是一个single token的projection。所以这里边我们就可以计算，就是假如你这个token找到了对应的专家之后，我们按专家去汇合这个token，专家里面激活的那些token，然后就整体的进去计算，所以他没有不涉及到token之间的一个interaction，就我们可以按专家对token进行分桶。这个讲完之后，我们最后再来说一个重点，就是不管是dense还是MOE参数量，这一块占大头的始终是FFN或者是M2P attention，是占比较小的。好我们再来看这样一个，就上一期我们介绍的这个A30，BA3BA3B这样一个参数参数量哈，它整体是30.5B我们在我们再看，我们再单独把这个GQA部分的和这个MOE部分的它的参数量做个对比，就大家可以看到这是很惊人的，呃对于MOE的一个千问3 30B而言，如果全量哈，就假如说这个我们这个128

因为128个expert，每个expert是三个，就是get、get up、down，分别是up、get、down。如果把它们做一个占比的统计的话，一个是0.29，一个是0.94。如果算激活的话，就是把128换成这个对，如果我们把这个算一下，先算一下激活的总参数量是33B，3.33B。我们把这个GQA部分拿出来，除以这个激活的参数量，把这个128换成八，也就是激活的时候MOE部分的一个参数量的对比，它是0.27比0.54。

我们再来看，再来对比看一下32B dense的模型，它的参数量。这个我就不带着大家去看了，我们统计一下，因为这里面就不分那个激活不激活了，因为全部都是参与激活的。统计看一下GQA部分的attention部分的和这个M2P部分的，他这个M2P就比较简单了，就64个，每个layer里面都有一个三个给up、get、down，5120到25600，这三个。GQA部分和这个FFN部分的一个占比是0.18比0.76。其实从占比的角度，这个MOE部分反而占比变低了，整体的占比变低了。

好，最后我还要讲一个事情，就是我可能漏掉了，在加上计算的时候可能漏掉了一个R，就是这个规划这个地方。我们知道RMSNorm，这是LLaMA TWO他提出来的这样一个normalize的操作。我们按维度去做一个normalization，那这里面有一个参数，MS路有个参数有个GA就是最skill，各位维度它是和这个维度相匹配的。这个伽马的大小，它是LINDBLOLD，不多一个参数，它和这个DEMODEL是匹配的。就是说每个层，我如果看代码的话，每个层我们看他前向的架构的推理的代码的话，它每个层这里就是transformer block，就是transformer layer。每个层里面有两个norm，在做GQA之前有一次norm，在FFN之前有一个norm，也就每个layer有两个norm。然后最终这个模型的最后有一个最终的final norm。所以就是每个layer，64个layer的话，假如对这个dense的模型而言，64个layer，每个layer是两个，然后地猫都是5120的，然后最后有一个5120。所以它最终的参数是，这个我们把原始的32B的参数量，再加到这个后边的里边，大概是这么多。好，这是对之前的一个补充。

关于另外的源码部分，就大家可以去这个Hugging Face的Transformers库里边，我们去看对应的千问三的一个前向推理的源码。就这里面我们其实也可以看到，在这个Transformers这个models文件夹里面，我们可以看到千问3 MOE、千问3 VR、千问3 VR MOE。这里边其实我想抛出来一个问题，在这个千问3 MOE和这个千问3 VR MOE的时候，我们可以看到，千问3 MOE训练和推理的forward的代码，就是MOE forward的代码是不分这个training和这个inference的。好，跳过去，这个代码没有变，它是不分training和inference的。

然后对于这个，还是之前说的，他是可以因为他是token level的，MOE是或者FFN是个token的projection，他是一个token level的，所以他是按expert去分桶。因为你就是我们在激活的时候，可能每个expert都有可能被激活，然后按expert去激活去算，按expert去分桶，去算哪些token是激活到了这个expert上。然后对于这个MOE的地方，他有点奇怪。

这里面我暂时没有想清楚，就是这个嵌入3VR的MOE export的部分。它是分了桶，不是，它是分了training和inference。training的时候还是走的是分桶的逻辑，但是在推理的时候，走的是一个BMM计算的逻辑。这个地方我不是特别清楚，大家如果都知道原理的话，可以分享出来。

好，以上就是本期的全部内容吧。整体我们这期重点介绍的是，首先整体梳理了各个参数的一个对比，然后主要是讲这个等效的MOE等效宽度，以及补充介绍了参数量计算的细节，包括ROS部分。

还有，模型这个GQA和MOE或者FFN，真正占大头的是MOE或FFN，不是GQA。GQA反而是占了比较小的一部分。好，以上就是本期的全部内容。好，以上就是本期的全部内容。