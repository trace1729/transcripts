# [CS主干SICP2004](唯一编程神作) MIT6.001计算机程序的构造和解释 · P17 17. Interpretation

[视频链接](https://www.bilibili.com/video/BV1TX4y157hX?p=17)

## 总结

- 构建解释器是编程的最终目标，它定义语言的语法和语义，本讲旨在实际构建一个评估器程序。
- 语言处理分为词法分析、解析、评估、打印等阶段，本讲聚焦评估器，使用Scheme组件。
- 从简单算术表达式开始，逐步添加定义、条件表达式、lambda和复合过程应用，构建Plan-Star语言。
- 评估器的核心是eval和apply的相互递归，eval基于类型标签分派处理不同表达式。
- 环境模型通过显式环境参数和表链实现，复合过程携带定义时环境，应用时创建新环境。
- 通过构建解释器，我们定义了一门语言，可用于解决其他问题；缺失部分可补充，核心框架完整。

## 大纲

1. 构建解释器的目标与动机 / 解释器的高层阶段概述
2. 建立简单评估器的初始步骤 / 评估过程的递归展开与原始操作
3. 添加定义与绑定支持
4. 添加条件表达式（if和大于）
5. 识别应用模式并简化评估器 / 应用程序的评估机制
6. 特殊形式（if）的评估与按需求值 / 评估器的调度顺序与特殊形式的必要性 / 将环境作为显式参数
7. 添加lambda表达式以创建复合过程 / 扩展apply以处理复合过程
8. lambda求值实例与过程对象的创建
9. 应用程序的求解过程与环境扩展 / 环境绑定与求值环境的关系
10. 环境链的实现与查找机制 / 解释器的定义与迭代行为 / 小语言缺失部分与方案解释器的等效性 / 定义解释器的意义

## 正文

### 1. 构建解释器的目标与动机 / 解释器的高层阶段概述

在接下来的几堂课中，我们的目标是建立一个口译员。基本意义是编程的终极目标，因为这样做可以使我们定义我们的语言，这是一个令人惊讶的声明，但是正如我们将在这些讲座中看到的，这是一个正确的原因。以下是我们用语言编写的每个表达式都具有的含义。与之相关的含义，推导与之相关的含义。表达是评估的过程，因此，如果我们可以定义为我们执行演绎的程序，我们对口译员的定义为我们提供了准确的规范，在我们的语言中什么是合法的，以及如何推断其中的表达含义。我们的语言，我们的目标是分阶段进行这种理解，探索一系列示例，最初是一些非常简单的示例，但最终以一个全面的计划评估者，我们将努力实现这一目标。

在继续之前，您将了解如何将不同的片段添加到口译员中。但是请注意，我建议在本讲座中附带一份代码讲义，您停止返回网页并打印出该代码的副本到在我们上课的过程中，先在您旁边，让我们设置我们为什么要这么做的阶段。为什么我们要建立翻译？为什么我们需要思考一下到目前为止在本课程中看到的内容，我们花费了大量时间讨论和使用抽象思想。程序和数据我们知道这是一个有力的主意，因为它可以让我们埋葬细节，它当然支持适当表达思想。级别设置了另一种我们可以考虑要做什么并将其分开的方式，从如何真正实现它的细节开始，但最终我们需要一个展开所有这些抽象以获得价值的过程。

对应于一个表达式，意味着我们需要实现解释表达式的语义会注意到我使用的单词，我说我们需要一个如果有这样的过程，那么展开这些抽象的过程，那么我们应该能够用我们的语言来描述它。描述流程，实际上您已经看到了该版本描述工作不只是一个过程，那描述是什么？环境模型，如果您认为有意义，刚刚描述了如何确定与表达式相关的含义。表达式只是展开了抽象到今天的原始操作，我们想谈一谈如何实际建立一个评估员作为一个程序，而不是一个抽象的环境。

首先模拟口译员的阶段，但针对的是我们正在讨论的方案，例如语言，通常有五种尽管它们也适用于许多其他语言。词法分析器，有一个解析器，有一个评估器，通常与环境协同工作，并且有一台打印机，让我们看看在我们构建它们并做到这一点之前，对它们进行高层次的研究。让我们来谈谈它们每个的输入和输出特性，专注于解释中每个后续阶段的输入，我们可以得到感觉应该一直发生什么，因此如何实际上建立一个评估器。

初始输入是一个字符串，表示我们要对此求值的表达式的打字版本，如果我们想要一个终端，那正是我们要在终端上键入的东西，计算表达式，所以初始输入是一个字符串。第一步是使用词法分析器转换该字符串，转换为单位或单词，此处显示的是将字符串转换为一组单词或孤立字符，如开放式括号和封闭式括号以及加号符号和数字，因此下一阶段的输入是一个有序序列这些单元或单词在第二阶段。然后将这些单词解析为一个结构，我们可以用于评估，尤其是我们可以转换单词的线性序列成树状结构这里我们为了方便起见使用对，但这不是要求，我们可以使用树的任何其他表示形式。

但是请注意，我们将要做的是将自我评估的表达式转化为内部价值，从而得到下一阶段是树的结构，悬挂在树的叶子上。我们的数字符号或其他以基本字词表示的对象和这是下一阶段的输入现在是口译员的核心。首先要注意树结构并弄清楚它的价值，树状结构的形式在这里，我们将进一步讨论，但是您已经看到我们如何转换或解析器如何将事物转换为树。每当我们看到开放印刷品表示新的开始时，如果已经在列表中，我们将创建一个新的列表结构，结构下降了一个水平，所以我们建立上一棵树，其中穿过树的每个水平切片对应于一些行动。

现在，评估者应该采取的行动是合并树形结构加上它和环境，并对其进行解释以及这是否意味着将环境视为一种联系方式。具有更原始值的名称，其作用就像字典一样，要做的是让评估者使用一组规则来遍历这棵树，与该词典中来自环境的符号相关联的值。

然后使用规则将复杂的表达式简化为简单的事物，最终得出一些简单的值，我们将返回该值成为输入。下一阶段的最后阶段是简单地将其转换的打印机，返回到适当的形式以显示在监视器上，然后只是显示给用户，所以这里是该过程的摘要。我们的目标是实施一个翻译程序，实际上确实，我们的目标是让您了解翻译人员的工作。要实现这一目标，实际上是因为口译员的关键部分是我们要集中精力的评估员，几乎完全基于这一点，我们将在所有其他我们将使用方案词法分析器，解析器和方案打印机，这意味着一门课程需要为一种看起来像滑雪的语言创建一个评估器。

### 2. 建立简单评估器的初始步骤 / 评估过程的递归展开与原始操作

具有树结构作为其解析器的输出以及一组用于操作该树结构作为处理实际情况的方法。评估还表明，我们将使用的语言语法演示所有这些，我们需要在外观上看起来很像方案。诸如使用括号来限制表达式之类的事情，例如我们所说的，这是因为我们不希望您对正在发生的事情感到困惑。方案以及我们如何看待建立评估者和评估者的想法，口译员我们的目标是建立一名口译员，尤其是评估员部分让您了解情况如何发生，并使用它来探索有关如何这些东西实现了语言规则，我们将建立自己的语言。

小语言，它是评估器，为方便起见，我们将其称为语言计划之星。正如我们刚才所说，它具有计划的许多特征描述过，但我们将看看如何构建自己的版本。我们将习惯于将星号放在末尾用我们的语言表达以区别于实际基础方案，所以我们将从一个非常简单的评估器开始。继续努力，第一个简单的评估者将负责简单的算术表达式，所以我们的计划是从构建一个处理算术表达式的求值器，实际上我们将要限制自己为两个或更少参数的算术表达式。

因此我们希望能够评估像此处显示的内容加上24到我们通过添加五和六得到的结果都会注意到符号结尾处的星号加两个表示这将是我们在此范围内建立的我们正在创建的少量语言。下面是一些代码，这些代码捕获了我们将评估这种形式的表达式，这是在此相同的代码与代码讲义分开，建议您随身携带该页面。通过这一步，我们现在将讨论此代码，但是在我们走时会使用它在查看示例时，请注意我们在这里所做的事情描述方案的知识，以描述表达式的求值过程，我们正在计划中使用这种新语言来描述该过程。

好吧，我们需要什么，我们有一个过程来评估月球中的表情，我们知道一种叫做eval的语言，它的形式是基本情况是一个仅由数字组成的表达式，要执行此操作它使用类型检查，然后我们可以在这里处理复合情况。它使用类型检查来查看是否有总和，并注意它的工作原理表达式的关键字，以确定该表达式的类型，如果表达式是一个和，然后我们将使用原始运算来添加子表达式的值的加法，但这里出现一个关键点。为了获得这些值，我们还需要评估每个子表达式，在这个阶段不知道它们只是数字还是复合的表达式，让我们先更详细地研究一下。

这个评估过程请记住，我们键入的表达式是转换为列表结构，就像符号和数字的树一样幻灯片上显示的内容，这就是作为我们的示例表达式的表示形式，因此让我们将其视为完全相同树结构被传递给Val，Val使用此输入做什么。如果Val抓取列表并测试其标签，我们将在讲义中检查代码这意味着它首先要检查整个事情是一个数字，因为它不是，它需要此列表的第一个元素结构并检查是否完成了特殊符号图。将类型调度到正确的过程来处理这种情况通过检查发送的标签确定它是一个和的表达式。

到元音歌曲，目前至少这只是正常过程应用程序，我们将此过程应用于表达式，所以现在eval将其减少为将eval sum应用于所示的树结构。请注意，等价金额是什么，它沿着树走下，抓住了两个如果Val和，则是该和的第一和第二部分的子表达式，然后将其转换为使用内置原语添加的内容评估物理子表达式，以及通过评估第二个主题，所以请注意我们所做的，我们将估值降低了一个。

在评估其他子项时，将表达式表达为一些简单的原始运算表达式。它做了什么？实际上只是从树上走了下来，表示该表达的结构，将正确的部分抽出。它使用了标签来告诉我们将其发送给谁，然后它就被抓住了。此清单结构的汽车和刀具，并将其交给新的评估。现在在这个阶段，eval de第一个对Val代码有效的子表达式，它会通过类型检查确定只是一个数字，然后简单地返回。哦，那很好，因为我们知道它只是指向数字24，所以数字24交还给另一块呢？

这看起来就像是我们开始评估某种东西，恰好代表总和的列表结构，该标签位于最前面，说这是一个加星号的东西，所以我们会做同样的事情，会将两个部分中的一些解开，或者对同一件事感到抱歉。不论我得到多少，评估总和都将简化为加两个的原始应用。通过评估子片段，这些子片段实际上是在走下坡路，树状结构抓住了第二个和第三个con细胞的四分之一，将val应用于这些对象。现在我们看到我们已经将此抽象展开为一些原始操作的应用原语，除了一些简单表达式仅是数字，这当然会减少为我们想要获得的最终价值。

但是要注意的关键是，这个小评估者是如何把它放在树上的，表示表达式的结构，并已连续两个将其展开评估，直到将其简化为简单的原始应用程序集，原始值的内置过程。因为这在以下阶段很重要，评估将更简单的事情分解为类型，正确处理程序的过程。现在让我们再看一次，着重于如何评估不明智的抽象，以及在每个返回的值再次进行评估。您可能会发现将代码用于该打印输出中一个在您面前的阀门，因此我们从这个完整的表达方式开始，在其前面加上小引号使我们脱颖而出。

我们知道列表结构是一种方便的表示方法，因为实际上我们所具有的表达式是用列表和树结构表示的，所以我们要以24星的元音+ 5星的6元开头。首先检查此表达式的类型，推导的不是数字，而是一个加号的东西，所以如果实际上将它分派给一个阀门加和，将其发送到为处理此类列表而设置的程序。现在的结构Val总和说，继续并通过利用每个，我们实际上并没有指定要执行的顺序，但为了方便起见，我们假设它从左到右移动，所以我们现在需要跟踪从树上下来，得到一个24的Val值。再次检查此类型表达式，推导它是一个数字，然后按字面意义返回该表达式，指向该事物的指针，它现在是eval的数字24，必须再次求值。

第二部分，所以这是+星5和6的列表结构的评估。在我们派遣类型检查之前，先看看它是哪种野兽，推论它不是一个数字，而是一个和，因此将其传递给正确的程序来处理。一次评估和将减少到应用，通过对子片段求值，我们得到的原始加法运算等。我们必须拿走这两块，然后再次对它们应用Val，在这里进行的很好的递归展开很简单。再一次，我们将对任何评估这两个部分的内容应用+，等于五次在类型上确定的调度，因为一个数字刚刚返回了数量和六个的评估，当然做同样的事情。

请注意，我们现阶段的位置已经解开了这个复杂的复合表达式，将其转换为基元的嵌套操作序列。我们知道如何处理的事物以及原始表达式数字，我们已经从字面上将其完全展开到了基本基本元素阶段。我们可以收集剩下要做的事情，并推迟，例如，我们必须在其中扮演最高角色，现在使用原始运算添加eval 5和l 6的值，以及将它们聚集在一起，以便现在将它们连接起来以累积推迟操作并返回我们想要的最终值。因此我们建立了一个处理不超过两个参数之和的简单评估器。

此练习中要注意的重点，因为我们的目标是了解评估过程。首先，val根据以下内容进行类型标签检查调度，就像我们之前在课程中看到的那样，输入第二个数字只是数字。到目前为止，我们已经使用了四个符号作为类型标记。表达式可以很好地递归求值，而eval可以解开复杂的表达式，转换成对较简单部分的评估，再加上递延将值收集在一起的操作。我们看到的数字，最终我们将整个事情简化为一组。

现在可以在此系统上构建原始值上的原始操作了。假设我们想给事物起个名字，例如，假设我们想拥有此处显示的行为，我们可以将中间结果存储为命名值，然后在任何我们想使用实际结果的地方使用它。这是我们在我们自己的方案中肯定看到的那种行为。我们是否会将其添加到我们正在构建的用于简单算术表达式的评估器中呢？当然，我们首先意识到的是，我们需要一种存储名称和值之间的绑定的方法。可以想象得到某物的值，但是现在我们必须有一种方法将定义存储在一起的过程，就是我们在小星球上创造的东西。语言必须有某种方式将各个部分粘合在一起，所以我们需要添加它。

### 3. 添加定义与绑定支持

这样，现在这就是讲义的第二页，不会被吓到。我们试图简单地突出显示我们已更改的内容，实际上是在讲义中，我们已经结束了到底发生了什么变化。在这里，它以粗体显示，因此我们必须首先添加我们需要拥有的另一个类型检查器，来检查我们是否有是定义的表达式，在本例中是定义的星号，因此我们添加了一个新的标签检查器。然后我们还必须在我们的标签中添加两个新标签，评估者。现在看一下新评估中的评估者，一种为事物创建名称的方法，当然，我们需要一种恢复他们价值的方法，因此我们在评估中又有两次派遣。大cond表达式表示如果这是使用内置符号的话，如果是符号，则使用方案检查器；如果是已定义的东西，则获取其值。我们已经检查了那个特殊的小标签作为特殊形式，然后我们将必须首先评估一下这种表达方式。

首先，我们将考虑处理定义了eval的查找的过程。我们需要的是将事物粘合在一起的方法，我们知道为此，我们假设一个称为表的数据抽象具有一个构造函数make table，它具有一种从表中获取内容的方法。给定一个表和一个符号，表get将使我们回到零，表示该符号不存在绑定或实际绑定存在。把东西放到表中的方式表放了，我们有一个绑定值，给定绑定的那个返回该配对的值部分。所有这些，我们可以简单地假设该表抽象存在，然后我们可以构建环境。让我们在基础方案中将基础方案变成一个表，然后我们可以创建查找程序。一个val定义查找一个表达式的值，如果它是一个符号，就是操作表的问题，我们进入表，找到它的绑定并返回绑定选票的那一部分，如果有的话绑定有价值的部分，或者否则抱怨。这仅仅是在操纵环境抽象。

说，我们添加的真实内容是处理新内容的一种方式，一种表达形式，是由定义好的恒星建立的，它正在创建绑定的名称和值。所以此程序应该如何操作才能遍历树结构弄出名字，记住这只是走树结构，这里没有实际评估，然后评估将提供绑定值的表达式。请注意eval的使用，递归返回到顶级和，一旦获得值，便使用相同的规则对表达式进行求值，代表环境的表格与名称绑定。因为我们已经向评估器添加了新组件，让我们来看一下如果我们在观察者中观察这两个表达式会发生什么。

特别是要查看每次我们递归地调用Val in时返回的结果。在此过程中，请跟踪参数并将Val的值返回为，我们使用代码讲义来跟踪此过程。现在让我们使用它扩展了小评估程序，看看当我们看几个表达式时，首先让我们评估该定义，找到X星的星号为加上星号四和五，这是解析器具有的树结构。解析器给了我们这个的树形表示，我在这里使用引号只是表明我想要与此等效的列表或树结构。瓦尔做这是一个调度，它说啊，这是一个定义，所以我第一件事要做的是再次对最后一个子表达式进行递归评估，以确定很好的估价。

这很像我们现在进行评估之前所看到的这个清单结构，我们从字面上走了，抓住这块，然后评估它的第一个表达式，这仅仅是递归确定表达式的类型和，并将其传递给Val sum。Val sum依次评估每个子表达式，然后应用基本加法操作返回一个值，因此该值由计算整个子表达式仅是9。然后val什么是定义说要记住，一个val定义是我们分派给的过程，因为那就是处理这种事情的那个啊，它的名字简直就是。

符号X星星从树形结构上抓住了它所需要的返回值，它将它们绑定到表中的某个位置。在该抽象定义的某处，它为名称X星创建了绑定和值9，并且在这种情况下完成工作后，它仅返回符号undefined告诉我们我们已经完成了。现在让我们评估一个表达式，包括这些名称之一，让我们珍惜X星的加星，并再次记住，这变成了树状结构，在这种情况下只是一个列表，三个符号和数字。我们在元音上检查标记，告诉我们这是一个加号，我们派发给Val歌曲的人说的Val是什么？它说啊把我递归得到的加在一起，评估子碎片的第一个评估X星，它是符号，因为我们抓住了列表结构的那一部分啊。

现在在我们的评估中，我们注意到第9行看到它是一个符号，我们只是进行查找，然后将其发送到进入表的表阻塞找到绑定，正确的星形返回该绑定，然后实际上拉出绑定的一部分，即值并返回给我们九分，然后我们将其加到两个，给我们11分。所以事实上，只要我们得到表的抽象，我们看到了如何向其中添加定义，以及仅仅通过表达式的树形结构如何让我们抓住在这种情况下，如果不对符号进行评估，则x星会消失，因此我们可以将其绑定到表中，除了其他所有内容仍然适用，这个小小的评估者，但是现在我们可以给已经扩展的事物起个名字。

通过添加此新功能，我们的评估师让我们退后一步并提取首先一些关键信息，注意到我们添加了两个新的调度评估程序的子句之一，只是分派给使用标准类型检查其他条款，但是依赖于使用符号用于检查名称的基础方案函数。为什么这个合理的注释是？我们依靠基础方案读取器将事物转换为解析树，特别是读者将转换X等字符序列在解析树中加入星号，然后传递给eval评估，将得到一个可以通过符号问号注意的结构。

第二件事，我们添加了一个表，或者我们添加了一个抽象的数据类型，代表我们的环境的表格。请注意，我们可以使用任何表格的实现，我们绝不依赖任何形式的细节表实现，而这正是我们创建方法的关键，只要有一种创建表的方式，我们就可以构建一个仅仅操纵他们的评估者。也要注意的关键是，我们现在已经为我们的小评估者添加了一种特殊形式，如果您仔细考虑，会注意到在此之前，我们只是评估正常，这种语言只是两个参数之和的组合。定义的功能有所不同，它仅递归地评估第二个子表达式，它是传递的解析树的第一个子树，在没有评估的情况下仅对符号进行处理，对第二个子树的评估。因此我们可以看到如何导入合并特殊形式输入评估人员。

最后注意到我们做出了决定，在设计我们的语言时，eval定义用于边的效果，创建某种类型的变量名和值的绑定，效果代表环境的结构。我们返回的值是真正取决于我们，在这种情况下，我们选择让它简单地返回一个特殊的未定义的价值表示工作已经完成，我们不在乎什么价值返回值。我们可能已经更改了设计，可能已经决定使用Val define总是返回与变量或变量关联的值，或者总是返回变量的名称，两者都是完美的合理的选择。

### 4. 添加条件表达式（if和大于）

我们还有什么可以补充我们的评估者的？直到至此，我们无法做出决定，我们不能根据值分支做不同的事情，所以让我们看看是否可以扩展以处理基本上是说我们可以扩展我们的小计算器，用于处理条件和ifs语句。例如：是的，然后做，否则做一些我们已经可以从一点看到的事情，我们在这里写的示例首先需要两种东西。我们将需要进行返回布尔值更大的星形的操作，应该比正常方案更大，应该评估一些表达式或实际上是一对表达式，并在值处返回true，第一个表达式大于第二个表达式，否则返回false。当然，除了大于，但我们要说的是，我们需要返回布尔值的操作。

第二件事，我们需要的是一个操作，它可以让我们控制分支，应该是一种特殊形式，首先应评估第一个子表达式，检查其值，然后根据该值是否为真或false会评估第二或第三子表达式，因此我们需要。

将if或branch的想法添加到我们的系统中，让我们遍历以下代码。在有相当数量之前，但很多只是早期的补充事情，以便我们可以先检查出发生了什么变化。我们将需要两种不同的调度方式：调度表达式是否是这些较大表达式之一，我们将像以前一样使用标签检查，如果是一个if表达式，当然，如果我们想要更大以外的东西，那么我们将不得不为这些添加单独的内容。以及我们还需要做些什么更改，我们只需在cond中再添加两个调度子句。这是我们评估更大表现力的一部分，如果表达式，所以这只是我们之前扩展我们的标准格式评估者，所以让我们先处理更大的表达式。

再记住，在exp中传递的是指向已由将解析器转换为树，该树结构中的第一个子表达式将是符号更大的星星，第二个子表达式将是其他表达式，而树的第三子表达式或第三部分将是另一个子表达式。请记住我们要执行的操作，我们要获取第二和第三部分的值，所以我们将使用树操作quatre和quatre沿着树走下来，抓住那些表达式，然后递归地使用eval评估它们，一旦有了这些值，就将它们传递给用于比较事物的基础方案原始操作，通过数字返回true或Valles故障的true或false值。

现在可能哎呀，看起来有些奇怪，为什么我们不只是使用大于直接使用的值，而是记住我们的目标是了解如何将评估器连接到内置基础原语，其次可能具有不同的处理方式他们。所以请注意我们在做什么，我们以递归的方式抓树，使用我们所有的东西评估它们以获得表达式的值，创建到目前为止，然后将这些值传递给原始过程，为我们返回一个真或假值。

如果表达式发生了什么，当我们再次派遣到一个有效的地方时，记住传递给该过程的是通过解析该树结构创建的树结构。标签有几片，如果是鲜明的，则代表表达式的谓词，然后是表示结果的树结构表达式，然后是表示替代项的树结构表达式。注意在我们的过程中我们如何使用列表操作来完成每棵树，然后我们评估第一个测试的价值，然后我们可以使用基本的原始机制分支以确定当时的测试是否正确，然后才得出结果并评估它是否为test，然后只有评估它。

注意完成的顺序，这是一种特殊形式，我们首先根据该值评估谓词，或者评估结果或备选方案，并将该值作为整个表达式的值。还可以，所以添加这两种新形式，这两种新的表达方式对我们的评估者来说更大，如果让我们看一些使用进行评估时会发生什么事的例子。因此，让我们评估将Y定义为9的表达式，然后看看当我们评估一个特定的if语句，如果Y更大而不是六个，我们想将Y加到两个，否则我们想返回15。

让我们追踪发生的情况，我们可以假设我们已经完成了对定义Y 9的评估，因为我们知道现在可以做什么，让我们来看一下评估表达式el要做的第一件事是检查该表达式是否为数字，它是一个和，它是一个符号，它是一个定义，它是否更大？检查标签以决定这是哪种表达的单词，由于标签check和最终决定这是一个if表达式。因此，如果现在回头看一下代码句柄，它将把它发送给Val。

您会意识到，如果先通过此树结构，将执行Val操作提取谓词表达式，即更大的y 6表达，并当然，这将评估该权利，我们已经将其视为一种特殊的形式，它将沿字面上的树结构抓住这个表达式，然后根据什么值进行评估。通过此评估返回，它将要么遍历树结构并抓住下一个子表达式并对其求值，否则它将走得更远向下树结构并获取最后一个子表达式并对其求值。特殊形式将首先对一件进行评估，然后分支到根据返回的内容对其他两个作品之一进行评估。

因此，现在我们可以很快地逐步完成评估，评估评估结果每次遍历此片段时，都会递归调用这个大的if表达式的值。在那里引用只是提醒我们是一个列表结构，我们可以看到它会分派给Val，通过走树走出谓词从句，当然不会。

然后执行另一次调度，它查找表达式并说啊，这是一个大于，这样得到子块的值，即我们得到走树形结构捕获一个Y星，然后六个递归地评估它们，然后当然，在这种情况下检查它们大于，因此我们返回树通知。顺便说一下，评估Y星将使用查找调度和评估6，将使用编号的返回茶，然后对总的来说，如果表达已经获得了真正的价值，我们将走向下到树结构的下一部分，并对其进行评估。将返回的值，这是加上y2的树结构的评估。在我们派遣这之前，这是一个总和，因此我们派遣得到y star的值，使用数字查找值2，然后将它们加在一起并返回。

我们想要的价值观可以通过走遍自己代码，并注意在每个阶段我们如何抓取特定内容，表示表达式并分派给a的阀门，再次使用适当的程序。让我们退后一步并提取从本练习笔记中获得的一些信息，评估磨碎器实际上就像Val歌曲一样，都在参数上递归调用eval，然后应用产生的原始过程。另一方面，我们的条件评估如果行为不同，这是一种特殊形式，而不是评估所有参数，仅按其中的特定顺序评估其中的一些参数，如介绍新的表达方式时，最后一个示例中的幻灯片。

### 5. 识别应用模式并简化评估器 / 应用程序的评估机制

在我们的评估器中，我们必须向eval中添加一个新的调度，我们创建了一个新类型，检查表达式，然后我们在con中添加了一个新的Clause，分派给过程来处理这种表达式。可以用数量很少，但是我们想添加很多操作，例如小于等于的乘幂等等，操作员，但仍保持Val紧凑，更重要的是便于我们以后进行扩展。我们已经暗示我们已经注意到像+和更大的运算非常相似，它们会评估所有参数，子表达式，然后对子表达式执行特定操作，结果值听起来像是普通模式，当然是这样，我们想要做的就是捕获该模式并利用它。

特别是我们将这个标准模式应用程序称为应用程序，将特定操作转换为一组值，因此我们可以做的是在我们的评估器中为所有应用程序实现一个案例，只是一种方式调度，这将使我们能够轻松地将其他业务扩展到我们的，只需创建它们并具有相同的评估器，匹配处理这种特殊类型的应用程序。这是我们计划的第一步，我们现在要进入并评估第一个子应用程序的表达式，请记住，在前面的案例中，我们没有对n加或更大，我们仅使用基础方案原语，没有对它进行评估。

我们现在就按照那种类型进行调度，并实际评估应用程序的第一个子表达式来决定正确的做法是，我们必须弄清楚如何访问，这样我们将在环境中为每个操作命名，希望将该名称的值视为一个过程，然后我们可以简单地将过程应用于操作数，其他子表达式的值。让我们看看如何去做，与以前的案例一样，我们扩展了我们的小评估者，看一下代码的打印输出，我们将逐步完成，再次强调我们对评估人员所做的更改，以便您看到我们如何发展事物。实际上在这种情况下，我们进行了许多更改，首先我们必须有一种方法来告诉我们是否有应用程序，我们将仅依赖于表达式是否为一对，如果组合是一个应用程序。

请注意，如果是这样的话，评估应用程序后，最好在我们检查特殊的事物之后执行，所以现在我们首先看到一种形式不断演变的形式，现在检查原始数字和符号，然后我们将检查复合表达式并检查是否有任何特殊形式的东西。到目前为止，我们已经遵守定义的正常评估规则，如果可以的话，检查我们是否有一个应用程序，确保我们确实有一个复合表达式，在这种情况下我们将继续进行应用程序通知。

我们现在已经删除了针对更大和更大的特殊检查，其他事情，因为我们不再需要它们，它们是应用程序，将被视为我们整体评估的一部分。然后注意如果我们有一个应用程序，我们将记住该树结构，传入的表达式是我们将要沿用的表达式，以获取第一个元素并对其进行评估，应使我们获得相当于过程的东西，我们接下来要走的过程剩下的所有元素，知道如何在这里使用may获取其他表达式的列表，因此可以是任意数量的参数，我们将在该列表下映射eval。

我们只是在对待地图是原始的，我们知道它的作用是进行评估并将其应用于列表中的每个元素，并创建一个新列表这些值的后面，那么我们将将该过程值应用于该列表论据，但要注意的关键是我们如何在树上行走您在每个零件上递归使用阀门的值，然后我们将做一些事情来递归地执行我们知道的应用程序如果我们的表达方式是名称的应用，凭证可以做正确的事情一些参数的过程，您将查找名称的值大概是附加到环境中的某些程序上，同样，将一个Valve向下映射到其他表达式的列表中将返回一个列表价值现在我们如何做一个应用程序从概念上讲检查运算符的第一个子表达式是否为我的简单计算器内置的东西之一，如果可以的话相应的原语，并应用于仅执行常规操作的参数这个原始应用的方案，所以要使用它，我只需要创建原始运算符，我可以使用抽象数据类型来实现存放我们从Scheme继承的基本过程基本内容我们将通过底层过程标记来创建原始对象它与原始类型，以便我可以检查标记定义的原始类型并获取来自数据抽象的实际过程很好记住，我们现在有一个环境有点表抽象包含绑定

因此，在开始使用我的小评估器进行任何操作之前，我可以在该环境中建立内置基元，例如，我可以将内置的原语，加上内置的原语，大于内置的原始true，它绑定到任何的Machine表示布尔值的真相将是这应该开始看起来熟悉的通知我们正在做的事情是建立一个包含以下绑定的环境：内建原始程序的内建名称程序标记它们的方式，以告知我们有一些程序创建，然后我们的评估者将能够使用那些内置的基本执行该过程的应用程序的原语传入的参数值没问题，让我们检查一下，让我们评估一下在我们的小评估器中，以下三个表达式将Z定义为值9我们将在加的值中做的很快会发生什么9和6，我们将检查if在其他对象上的评估我们将检查申请表和特殊表格，以使确保他们都在我们首先创建的这个小评估器中工作

让我们检查一下环境后的样子对前两个表达式的求值还记得它只是一个抽象数据类型表中具有名称和值绑定的表，将Z星号绑定到来自定义评估的九个，我们知道我们也会有真正的作品绑定到真理的布尔值的基本表示形式，因为是我们内置到环境中的钞票管之一，否则我们是否将环境放置得很好，我们为与更大的恒星和正星相关的原始过程，这些过程我们选择代表它们的方式只是符号基元的列表告诉我们这是一种什么样的东西，以及指向基本内容的指针机器操作的加法和检查是否大于此我们的环境看起来就像我们拥有的价值观的绑定

我们现在要使用它，因为我们现在评估另外两个表达式做一个加号如果可以，让我们逐步完成简单的评估阶段现在和以前一样，我们建议您使用第四页的代码句柄现在当您跟随您前进时，我们将从评估树开始与加九六引号相关的结构那里的游戏只是在提醒我们如果瓦尔走过一个大树，那是我们要走的树结构分派检查是否是数字是否是符号是否违抗是否是这些都不是真的，所以它说必须是一个应用程序，或者说是检查确保它是一个应用程序，它是一对，它是树结构，因此将要开始并进行应用，所以eval将其分解为以下应用我们通过评估树的第一部分而获得的结果以及您获得的结果将Val映射到树的其余部分，并注意此处的表单穿过树，抓住碎片并递归地将Val应用于这些碎片中的每一个碎片

所以符号加星号的评估会发生什么好吧，Val走过，说是数字，不是符号，是的，所以它会在环境中进行查找并记住我们所看到的来自环境，只是名称的绑定加星号到列表结构，所以这是返回到的列表结构标签原语和指向我们完成的原语加法操作的指针现在评估的第一部分，我们有了操作员的价值，所以现在，我们可以继续进行映射，如果要减少其他片段的列表，变成今晚应用阀门并将阀门应用到六个阀门的列表

当然，我们知道每个表达式的作用是什么。事物的类型是数字时，在这种情况下，它只是返回它知道自己正在评估的表达式，因此只为我们返回了九个列表。还有六个注意我们现在拥有的表单，我们有一个表示形式的应用程序，包括原始过程的列表结构和值列表。现在就开始并执行apply，apply首先检查参数以确保它是一个具有正确标记的东西，因此现在将其减少到与ant相关联的过程的基础方案应用值，然后减少到以下值，所以我们现在有了应用基础方案值列表的加法运算。

我们会注意到我们已经完成的工作，表达式的求值并将其简化为另一组表达式的应用，递归地评估过程和参数列表的值。如果参数本身就是组合，那么他们本来可以解散它，直到我们将表达式的评估减少为将基础基本图元应用于值列表。当然，这只是在做正确的事情，它完成了对我们的回报。我们期望关键看到的价值不是它可以增加9和6，而是将组合的评估简化为将过程应用于一组现在可能本身具有的参数，这些参数又可以减少到其他类型的评估。

### 6. 特殊形式（if）的评估与按需求值 / 评估器的调度顺序与特殊形式的必要性 / 将环境作为显式参数

放松可以的例子证明我们的程序正常应用将在这小部分工作，评估程序让我们检查一下，以确保特殊表格仍然有效。让我们看看如果我们很好地评估了if语句会发生什么，我们当然知道当在游戏中传递时会发生什么。我们提醒您报价有没有显示这是一个树状结构和评估步行向下是一组可能性，说这是一个数字，这是一个符号，这是一个挑衅。

是这是否如果它确定不是它，所以它将分派转而处理将要减少为在此表达式上使用Val if并记住if提取的值是什么。如果您首先说的是Val，那么代码就在您面前，评估第一部分，并记住我们把这个表达式当作一棵树，我们抓住了关闭第一个子表达式，将if之后的事物作为符号并对其求值，或者如果您喜欢并对其进行评估，则具有树结构，我们将给出名称测试关键点的特定值。

我们首先评估在查看此表达式的其他任何内容之前，树的一部分游戏检查其可能性，检查是否是数字，不是符号，因此它会进行查找，并进入环境并找到在该表中绑定真正的星号符号，这当然是现在将有真理的基本布尔表示形式。获得了此查找的值，然后可以进行条件和记住在eval的内部，如果我们说基于测试的值，要么评估结果或替代，所以现在它遍历了树的其余部分结构并抓取想要的部分，然后减少到简单在此表达式的Val中执行此操作所返回的值将是整体表达，这当然很简单，我们知道这是一个数字，因此它仅返回表达式本身。

需要注意的关键是if的值如何遍历表示该值的树结构，总的来说，如果表达式按特定顺序排列，则它的值会减小，如果表达为其中一件的价值，那也很关键，要注意我们没有走遍所有的树，只评估了我们所有的碎片按需评估的申请与实际适用的申请截然不同。在这种情况下永远不会被调用，我们只会对其中一件如果有申请我们会做，但就件而言本身，如果是可以处理的特殊形式，我们绝不做任何申请以特定顺序评估子零件，但从不依赖于应用程序。

如果不是的话，这是一种特殊的形式，让我们再退一步，我们在这里添加了一些内容，以了解我们可以从关键观察中获得什么。我们现在扩展了一些处理应用程序的评估器，这意味着一旦我们谈论其他细节，我们就可以原则上创建各种表达方式。主要要点是请注意，应用程序必须是eval内我们分派中的最后一种情况，我们不在这里检查标签，我们只是依靠它不是特殊的特殊形式或原始形式，我们必须将其视为应用程序。

还请注意评估形式，在其中我们检查特殊内容比较原始的第一件事，例如数字和符号，然后检查不符合正常规则的特殊形式的东西，例如if and then，检查以确保它是一个应用程序，并使用常规机制应用程序。正如我们刚刚说过的那样，当我们查看第38行中的expression评估其所有子表达式，然后将第一个值应用到它的正常形式的参数，需要特殊处理特殊形式的表达式，例如获取它们。

需要特殊处理特殊形式的表达式，例如获取它们。

评估自己的情况并分别做一些事，为什么在我们寻找之前必须先对它们进行治疗？尽管如此，我们现在已经看到了一种基本的方法，构建一个小评估器。我们有了原始表达式，原始的组合方式，得到了原始的方式，有条件的，得到了进行申请的原始方法，使我们能够进行。下次我们将在评估器中创建其他类型的基本表达式，在我们发展的下一阶段进一步扩展评估人员。

很少有人评估将环境作为一个明确的参数，到现在为止，我们只能依靠一个环境来存储所有不同变量的绑定，实际上这是一个全局变量，具有全局变量的过程名称的环境。实际上，我们即将开始走向应用程序，评估程序的先前版本。我们现在有了程序的应用程序，我们知道要去哪里，我们将需要能够在任意地方使用，这将需要本地环境。因此，让我们明确地退出我们的环境，我们必须将评估者从只需评估表达式，改为评估关于在某些环境变化上的表达式。这将需要所有程序使用额外的参数调用，并查找和定义将要使用的参数，从论证环境。

这实际上是一个真正无聊的更改，此评估程序的功能与我们想要的完全相同。在第四个版本的两个中，我们要做的是将延长阀取下，把表达式和环境作为参数，然后确保所有调度函数，如lookup eval，会定义eval，如果将环境作为参数，函数诸如lookup eval会定义eval，以及对eval的任何递归调用以及其他更改。当我们做一个关于什么环境我们不得不说，最初的环境必然是包含以下内容的全局环境，内置过程的绑定以及我们进行的其他定义。

比这只是簿记，它使我们得以扩展。实际上，我们将要采用的评估师的一般方式是，唯一不平凡的情况是在eval中处理应用程序。记住，这是说这是应用程序还是组合的调度，涉及过程应用程序，我们必须应用代表其参数的过程。注意到我们必须像以前那样做，计算该树状结构的第一个子表达式以得出程序，我们将扩展Val以采用此参数的环境，为了获得其他表达式的值。记住之前，只需在列表上映射一个有效的表达式的编码器，这里我们必须是稍微小心一点，我们必须实际评估每个部分，该树相对于环境的一部分。

因此，我们将使用这里的lambda实际捕获了该过程，我们将回到很快，但是您可以从方案的角度看到这很有意义，映射一个评估每个子表达式的过程，将其从关于传入环境的树中取出。这是唯一真正的对于扩展评估程序的情况，我们需要进行非平凡的扩展，明确地考虑环境。我们已经准备好使最后一个大的特别是我们的解释器的扩展，我们希望能够添加新的程序。如果您考虑一下，一旦我们具备了这种能力，基本上创建了我们可以处理的所有解释程序的各个部分，表达式，我们可以处理特殊形式，我们可以处理应用程序，只要我们可以创建自己的程序，然后允许根据自己和其他方面。

### 7. 添加lambda表达式以创建复合过程 / 扩展apply以处理复合过程

那么我们如何添加新的程序，以及我们当然想做的事情是定义一个名称，对于我们要创建的相当于lambda的东西，我们自己的过程，方法是将其捕获为lambda表达式，然后用它来做到这一点。那么我们需要添加到我们的小评估者，我们的小解释器能够更早地捕获过程中的计算。在术语中，当我们谈论方案本身时，即lambda表达式捕获了一种常见的计算模式。现在我们将了解通过创建将要处理的内容来将这种能力添加到评估者，在我们的新小评估程序中使用lambda表达式来执行此操作。

我们需要首先要做几件事，我们需要在评估器中再进行一次调度，处理lambda表达式的东西。我们喜欢lambda表达式返回一个复合过程，无论我们决定如何定义。第二个，一旦我们有能力创建自己的程序，我们现在需要扩展apply以处理此类程序，只需设置我们继承自其中的内置句柄原语，方案，但我们还必须能够处理我们创建的过程自己。

然后我们实际上应该回去完成环境模型，实际完成的细节，因为现在它将成为如何我们将自己制作的程序应用于变量，并确保评估的进展会酌情进行。因此，让我们遵循以这种方式扩展我们的评估者的策略，让我们从寻找所需的内容开始。

像以前一样添加到我们的代码中，我们建议您打印看看，所以让我们从我们必须改变的角度来看它。鉴于我们已经拥有的东西，在到eval的调度中非常简单看到了，我们将需要一种新的方式来检查标签以查看是否有lambda表达事物将为我们创建一个过程，当然我们会必须添加到eval的另一个调度中，该调度指出是否为lambda，然后顺便说一句，我们必须补充一点，它必须在应用程序，因为这是我们的基准默认值，所以任何东西都是一对，但不是我们将其视为一种特殊形式的应用程序，因此需要lambda在那之前，它真的也应该在检查原语或基本形式，例如数字和符号，因此属于另一种形式特殊形式，例如define和只要没有排序就不重要它现在显示在本节的某个地方。

Val lambda应该记住什么，如果我们将评估表达式作为具有作为符号的第一个子表达式，lambda符号开始说这是使整个树结构传递到的过程Val lambda以及环境，我们想要做的好，首先，我们需要沿着树形结构走，并抓住表达将在四分之一处，我们想走下去，抓住将在Cata der上拉过的那两个表情的主体，只需将这些零件拉下即可将其拆下而无需评估通知然后我们想要将树的列表或某种表示形式粘合在一起的树，听起来应该熟悉的关于身体和环境的争论。

这听起来很像实际上我们在进行评估时所拥有的在环境模型中的双气泡符号，包含哪些参数或参数的过程身体是什么，它在什么环境中创造并制造化合物，简直就是将这些片段粘合在一起，请注意，明确没有调用Val在Val lambda内部，这些表达式都没有评估他们只是被当作树结构对待。注意，我们已经使用katha决定了程序的主体表达式的门，我们明确希望主体包含一个表达式而不是事物的列表，我们可以更改它，因为我们会看到一点稍后在该术语中添加新表单以创建lambda表达式，创建过程对象非常简单，我们必须回来一秒钟就能制成化合物，但这没什么大不了的。

现在的问题是如何更改申请，并记住申请将评估树结构的第一个子表达式，找出合适的过程评估所有参数，并获取这些值的列表，然后在原始情况下，我们希望将该过程应用于该列表从计划中继承的东西，例如加倍大于它的问题，只是做基本的操作，现在我们要扩展功能，让我们看一下在申请时仔细考虑如何扩展这种情况。现在，操作数列表中的一个运算符需要检查类型是否正确，首先检查一下，这是否是原始操作符，在这种情况下，我们只是应用了适当的基础方案程序。

另一方面，如果这是一个复合运算符，即我们正在评估其中的表达式使用的是我们在评估程序中使用lambda star创建的过程，如果将其应用于一组参数，我们需要了解如何使进行评估，在这里注意到我们在做什么，我们只是实施环境模型的想法，那么我们该怎么做才能得到身体，当我们拉出我们粘在一起的那一部分时做了lambda表达式，我们将对其进行评估，但我们将现在就环境来评估它，因为那就是我们需要什么，但是将通过获取操作数来创建该环境这是在采用运算符和绑定参数时传递的值，在一个新的环境中将它们一起创建前一个并返回一个我们可以相对的环境当我们进行复合运算时，可以以更短的方式进行评估，我们将抓住身体并在一个新环境中对其进行评估我们在先前环境中拥有的东西加上它对将过程的参数转换为在新框架注释中传递的值。

这与应用创建的方式一样，通过这种方式我们现在有了一个很棒的表达式相对于环境的循环结构评估一般情况将简化为将过程应用于一组参数，并且在一般情况下，将减少评估新表达式的主体关于新环境的运营商，通过这种方式，Val和apply将在解开过程到基本的原始操作和原始表示数字和符号的余下要做的就是实际实现double bubble概念是抽象数据类型，我们可以通过为……

### 8. lambda求值实例与过程对象的创建

复合物，然后使复合物简单地将它们粘合在一起，在带有适当谓词和两个选择器的列表中。碎片，当然我们无论如何都可以做到这一点，重要的是我们拥有将构成等同于两倍的东西的东西粘合在一起的某种方式。我们通过评估代码的lambda返回的气泡肯定是，一种了解我们已经完成的工作的方式，但是通过以下方式可能更容易理解，观看表达式评估的解决方案。因此，让我们看看当我们评估一个lambda表达式时会发生什么

评估列表结构，其第一个元素是符号lambda星，标签，告诉我们所拥有的与之相关的参数列表，以及身体，当然我们正在针对某些方面进行评估，我们将这个环境称为全球环境。现在我们建议您遵循第六部分的代码，因为请注意，如果Val采用该树结构中的符号列表和其他另一个这样的列表结构，它检查它是否是一个数字，一个符号，两种原始形式是已定义的，还是另一种形式的if特殊形式。这些都不是真的最终要检查看看这个lambda，当然还有第一个元素的标签检查说，因此，这表示我们将将此表达式传递给eval lambda，设置特定过程以评估lambda表达式或lambda特殊形式。因此eval lambda可以得到与上述相同的树结构以及全局环境输入，并注意它说要执行的操作，然后说出要走下的树状结构，抢占四分之一或第二个子表达式的参数。

树结构类似地得到主体，类别或第三个子表达式，我们再次假设只有一个在那里表达，所以四分位数将使我们离开x星类别的清单，将使我们脱离名单，再加上X星X星X再次注意，我们只是将其作为树结构得到了这两部分，然后我们想要使用合成胶将它们粘合在一起，因此评估减少到了表示这当然只是使用我们的构造函数来胶合这些，并按照我们选择的特定方式实现转换为这个。现在我们要转到一个基本的原始构造一个新的列表结构，我们一直认为可以做的事情之一，带有标签标签的化合物以及参数列表和主体表达式，作为创建此lambda星型表达式的环境，并且说此表达式的求值返回此列表结构，这实际上非常好，我们已经用语言创建了一个新对象，我们评估了一个λ星并创建了一个过程对象。

当我们去做一个应用程序时，我们需要一个表示为列表的表达式，我们可以拉起碎片的结构，在这里它们都是应用程序所需的部分，在列表中表示为列表结构，该列表以便我们可以轻松地使用评估器中的那些，因此列表结构代表一个过程，这是我们的特殊方式。为过程创建数据结构时，它会带有一个标签，说它是一个复合过程，它具有一组参数，并且具有表达式或正文，全部以我们期望的格式表示为解析树，所以现在我们当然可以在我们的小解释器中创建程序。

也给它起个名字，我们知道该怎么做，我们要评估一个我们在其中定义名称的表达式，该值具有与任何内容关联的值。程序对象都是相对于我们可以放入的环境完成的，一起看这应该发生什么。首先，我们要在我们的环境中有一些初始绑定，所以这是我们的抽象表，在这种情况下代表环境的全球环境。例如，我们将Z星的名称绑定为9，将真星的名称绑定为初始绑定到布尔值表示形式的初始。

的确，在这个小环境中，我们可能还会有其他绑定，例如，如果我们之前定义plus并将其添加为原始元素，我们将有一个对于plus具有约束力。我们从之前的探索中看到的是plus作为名称将在环境中绑定，以指向在这种情况下，原始基本过程带有标记，表明它是一个原语，以及指向任何代码的指针将做正确的事情。记住我们已经安装了它，或者必须将它安装到环境中，当我们加载原始环境时，尽管如此，绑定将在这种全球环境中对我们可用，所以这说实际上，该表中的加星号将指向实际的。

现在，当我们评估表达式时会发生什么，在顶部进行评估，以了解哪种这是确定的表达式，是否已定义并将其发送给为处理将要创建的特殊表格而设置的程序，在此环境中两次绑定到通过评估。

Lambda星级表达，我们当然知道Lambda星级评估的方向，返回的就是我们刚刚看到的东西，它使我们回到了该结构。这里要注意的唯一问题是环境部分——实际上，它指向了这个全球环境。我们进行了评估，当然还对名称进行了两次绑定，价值将在该全球环境中发生，并且到该表将建立两个星号的符号配对以及指向整个符号的指针结构，这就是我们对过程的表示。因此请注意，现在发生了，我们已经创建了为程序中的过程创建名称的功能，环境指向这些过程对象之一，这意味着我们现在可以通过名称引用该过程对象，从而建立一个抽象级别。

### 9. 应用程序的求解过程与环境扩展 / 环境绑定与求值环境的关系

让我们看一下表达式的计算结果，看看它的用法涉及这些程序之一的名称，尤其让我们来看一下Val和应用程序如何协同工作来解开抽象并获得帮助，以基本情况为例。因此首先，我们要评估此表达式到全球环境，eval会推断出这是一个应用程序，不是一种特殊形式，所以评估此表达式可简化为一个必须在其中求值第一个子表达式的应用程序，进行树结构操纵，并将其应用于通过映射得到的任何结果，评估初始输入中的所有其他其余表达式，并针对全球环境。

那么这些零件的评估效果如何呢？我们知道这是Val数字的象征，它只是在环境中查找，为我们返回了捕获这些碎片的列表结构，标签作为复合物，此参数列出其主体以及它是在使用该映射评估参数时创建的环境，给我们返回了一个四号列表，因为对一个数字求值只是返回一个数字。因此，立即应用将应用此表示形式的复合过程到参数列表。如果我们看一下讲义中的代码应用，我们看到说使用列表操作抓取形式参数的参数，采用传入的值列表并粘合，相对于原始环境而言，该过程必然会创建一个新的列表框，我们还没有去做，但我们一定会，但是我们当然理解从概念上讲，它要做的是用正式的参数创建一个新的环境，绑定到参数列表且受环境限制的参数，在其中创建教师对象。

然后创建什么，我们也会这样做吗？完成程序的主体，我们将评估该新环境中的表达式。请注意，该表达式只是列表操作，我们还没有评估我们为此设置的任何部分，因此扩展环境将是某种抽象数据类型，创建新环境的操作，我们称其为这些东西已经束缚了，我们现在将对该环境中的表达式进行评估。所以从概念上讲，我们只是在这里有了新的某种新结构，在其中我们有了新的内部环境，一个新表，该表中包含指向最初的全球环境的指针。现在我们要评估在这里的表达，请注意我们如何减少对某个表达式的评估，关于新环境的两个简单表达式的环境评估，扩展环境，如果您愿意，有一个不错的基本周期，环境模型，当然还有我们评估人员的基本周期，在我们继续对该表达式的求值进行跟踪之前，创建了对这个新环境的新的更简单的表达式。

让我们退后一步，看一个重要的点。假设我们开始对在全球环境中两次失败，但在另一些环境中发生得很好的应用程序，必须做子表达式的评估，以及那些也将与此新环境有关，因为它已作为评估的一部分传递下来，因此这意味着在此阶段我们将使用相同的其他新环境。但是当我们进入实际的应用程序时，注意这里的环境不会改变，因为这里的全球环境来自程序的创建，实际评估了lambda表达式，我们建立了这种复合结构，其中包含指向环境的指针，被创建，这意味着对身体的评估将会扩展这个环境，不是我们所谓的环境，即使我们可以调用该表达式在任何环境中被过程正是创建它的过程。

现在让我们结束找出表达式的求值。我们现在有了一个新的环境，我们刚刚创建的一个名称，该名称的本地绑定是x star 4，并且受全球环境影响。在该环境中，我们正在评估该过程的主体，以及X星X明星，但我们当然不知道那些程序的主体，只需解开评估——使用此表达式的评估尊重环境。我们现在可以快速完成这些步骤，这将再次推断出它是一个应用程序，我们将评估第一个程序，以及进行映射和评估的另一部分，取得第一部分的片段，我们只需查找绑定。

原始事物，地图将简单地查找a1中X星的绑定，找出实际上会减少应用价值的那些价值。原始过程到值列表，然后我们应用使用基本方案的基础方案成为简单的应用程序。一组参数的过程。好吧，唯一要做的就是实际实现环境模型本身，不同的环境。我们现在只需要在这里做出一些选择，将其视为抽象数据类型，但实际上我们可以做得更多。为了这个博览会的目的，我们可以选择将环境表示为表列表，我们将使用表抽象，和以前一样。我们知道我们可以按照清单来实现结构，以及环境只是表序列的列表。

### 10. 环境链的实现与查找机制 / 解释器的定义与迭代行为 / 小语言缺失部分与方案解释器的等效性 / 定义解释器的意义

因此，例如，考虑到环境是表格的嵌套表链，例如从框架a开始，该表受全局环境限制，这也是我们可以简单地表示如下。现在是一个环境，指向列表的第一个元素是第一帧，第二个元素其中的元素指向剩余的表列表，这在过程中可能任意长，所有这些都将终止于全球环境。这意味着在任何地方都没有二氧化碳指针，该环境将包含安装过程中建立的所有事物的绑定，这只是原始环境的一种抽象方式。代表环境，我们可以看到它如何嵌套或排列在一起，环境链。这将允许一些课程来构建一个简单的实现，这里是实际实现大多数环境的代码。这只是使用创建初始全局列表的簿记详细信息，环境仅仅是从一个空的环境开始的问题，然后建立一个新框架来创建新的配对，对于具有原始图元实际表示形式的内置过程。

否则，我们需要改变一下吗？现在看起来要聪明一点，或者在我们开始的环境中查找值要明确得多。俗话说这个环境是空的，还记得一个环境现在是什么。如果没有任何可能的帧，我们就会遇到问题，我们会抱怨关于它，我们显然有一个未绑定的变量。否则我们会做我们所做的，之前唯一的区别是我们在表格中查找了表格环境的第一部分，汽车的第一帧将其拉出，没有绑定，然后我们递归地继续进行查找环境的其余部分，是环境的核心。我们将获得下一帧，所以如果您考虑这一点，只需使用标准列表操作，其中列表现在是表或环境，直到找到绑定为止。

我们要做的唯一其他更改是定义定义，我们知道将改变环境中的第一帧，因此在我们的评估定义表达式中，当我们做一个表时，我们将其放入第一个在此环境序列中的框架，以那里的列表表示，拔出Karva，这为我们提供了改变的正确框架。事情又退后一步，看看这个，这只是在操纵列表框架，以创建一系列环境并确保我们先在那个环境序列中进行查找，直到我们找到它。因此，我们已经实现了解释器，需要注意的关键是应用程序评估发生的循环，并从核心应用此评估程序的eval在环境中采用表达式并减少，这是将运算符应用于一组值，这些值又减少为评估关于新环境的新表达方式。他们继续这样做，循环展开过程的抽象，直到减少了对原始组件的表达，尤其要注意。

没有任一调用上的挂起操作，val没有任何挂起操作，并且本质上是迭代应用。类似地，没有待处理的操作，并且因此，如果我们评估一个迭代过程，我们将得到一个迭代行为。因此，事实上我们似乎已经尽了最大努力，那种成熟的评估者或口译者。小小的人仍然缺少什么，计划星的东西，我们只建造了几样东西，我们如果您返回则无法计算表达式序列。并观察一下我们在评估程序时所做的事情，假设身体只是一个单一的表达，而不是我们知道的序列，我们可以有一个正常的方案，所以我们可以考虑在其中添加一些内容，会做到这一点，这将是一种新的特殊形式，但我们可以轻松处理。

我们缺少的另一件事，我们只有几个数据类型，我们只有数字、布尔值，关于添加诸如字符串之类的东西，再次我们可以做到。但这实际上是仅有的两件事，从我们的小计划明星解释器中丢失了，然后导致专业本课的重点是，如果我们只是从名称中删除星星，这意味着它表示我们已经描述了方案中评估和解释的过程。我们实际上已经建立了一个方案解释器，它不如一个成熟的东西，但是缺少的东西实际上很小。如果我们删除了表示，可以建立一个方案解释器，我们不需要在方案之上做，但是我们可以。

已经在一种更原始的语言之上做到了，或者我们可以在其他一些语言，这是定义评估器的重点或口译员，我们正在定义语言，我们正在指定含义评估这种语言的表达方式Legal中的合法语法是什么语义，一旦完成，我们就创建了一种可以使用的语言为了解决其他问题，我们将回到这一点，但这是实话。