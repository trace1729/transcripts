# UCanBuildAnything·2940·一口气上手Supabase+Resend开源邮件自动化
[视频链接](https://www.bilibili.com/video/BV1VazHBEE1Z)

## 总结

- 教程旨在教授使用Supabase、Resend和Stripe搭建自动化邮件工作流，涵盖注册欢迎、支付和取消订阅等场景。
- 项目基于一个开源的MVP启动模板，并新增了邮件自动化功能，用户可克隆并进行定制。
- 详细讲解了需要预先配置的环境，包括Supabase项目、Stripe密钥、Resend域名和API密钥。
- 核心实现是基于Supabase Edge Functions和数据库触发器，通过pg_net扩展在数据库事件触发时调用函数发送邮件。
- 通过复用仓库中的SQL脚本，能够快速创建所需的表、触发器和边缘函数，实现完整流程。
- 最后强调了部署和测试的步骤，并提及了在实际应用中排查错误和上手体验的重要性。

## 大纲

1. 开场介绍与演示效果
2. 项目来源与快速上手准备
3. 配置Resend服务
4. 设置环境变量与启用pg_net
5. 创建并部署边缘函数
6. 创建数据库触发器
7. 部署、测试与总结

## 正文

### 1. 开场介绍与演示效果

每个人都是潜在客户，所以今天我想教大家如何设置自动化的邮件工作流。我们将涵盖95%使用所需的技术：超级基地、边缘函数和触发器，以及Resend。正如屏幕所示，这是一个开发者工具邮件，在这些功能之上，最重要的是要理解为何需要欢迎邮件工作流。因为当用户注册您的SaaS产品或AI初创公司时，他们可能在寻找下一步该做什么。欢迎邮件是与用户互动的好方法，这些用户已经对产品表现出兴趣。另一方面，如果他们已经付费，需要发送确认邮件；如果他们取消订阅，可以发送邮件挽回，与他们互动，了解某些需求未被满足的原因。

让我们直接开始。好的，新用户刚刚完成注册，同时我的Gmail收到了欢迎邮件。我可以点击查看，欢迎使用Launch MVP。如果前往超级基地并查看名为user_underscore_email的表、underscore_log，可以看到该用户已收到欢迎邮件，状态显示邮件已发送。点击开始试用，我们来检查一下，如果一切正常在此输入卡号并支付，我将在其他界面操作。好的，我已完成支付并刷新页面，太好了，我们还收到了新邮件，已收到付款通知。也可以尝试点击取消订阅，如果这样做，是的，取消后可恢复订阅，用户会收到订阅取消通知邮件。现在可重新订阅，点击即可。返回超级基地，可以看到欢迎邮件、取消邮件和账单确认邮件已发送至该邮箱。

### 2. 项目来源与快速上手准备

部分老观众可能熟悉这个git仓库，这是我去年开源的项目，这是一个完整的MVP启动模板，基于超级基础、Next.js、Stripe集成。现在我们又添加了Resend服务，我们将继续使用这个git仓库，我们发布了新版本，称为双邮件自动化。如果你向下滚动到这里顶部，可以点击尝试应用，然后你会进入这个着陆页。有些人可能会发现它和我的产品有些相似，Automatic.io。我们在此开源项目基础上进行了升级和扩展，但本质上你知道的，这是一个所有人都可以参与的项目，我们欢迎任何人加入并提交代码。

现在我将详细讲解如何为你的产品搭建。如果你是第一次尝试使用超级基础和Stripe，强烈建议访问这个GitHub仓库，然后点击顶部的视频。对的，我们已经到了关键步骤，音乐，不是这个人在做广告，你将看到我演示如何设置这个应用。可以观看之前的视频，但简要说明的话，你需要克隆这个仓库，给它起个名字，然后直接下载，将其转换为你的专属仓库。然后按照这些步骤操作：创建新的.dot m loco文件，然后配置谷歌云服务。将谷歌云凭证输入subbase用于登录，还需要创建你的超级基础项目。

需要预先创建处理新用户的函数，只需运行名为initial supervised table schema的文件，SQL，复制这段代码，粘贴到超级基础编辑器。所以表格结构大致如下，除了新增的用户邮件日志表。当用户通过认证登录时，会将用户添加到用户表、用户试用和用户偏好表。当然如果用户付费，会被添加到订阅列表，之后用户会收到邮件并记录在用户邮件日志表。完成超级基础配置后，同时还有设置Stripe账户的区域，然后将密钥复制到我们预先准备好的所有环境变量中，M dot example。欢迎随时查看，今天我们直接进入正题，也就是设置邮件自动化流程。

### 3. 配置Resend服务

好的，我已经在这里详细记录了你需要的所有步骤，在公开仓库中，请先阅读README。但最重要的是要记住我们使用了数据库触发器和边缘函数，以及Resend服务。对于Super Base，我们还使用了一个叫Super Base Secrets的功能，这与部署到前端的方式有所不同。因为我们正在创建帮助发送邮件的函数，这些函数位于Super Base的边缘函数中，因此它们也需要密钥，特别是来自Resend的，以便Super Base能够访问，或代表你发送邮件的权限。现在开始设置Resend，然后看看效果如何。

首先你需要登录，好的，我将使用Google登录。登录后，你将进入仪表盘，新用户需要完成几项操作，首先是设置，设置名为域名的功能。

如果你有自己的创业公司域名或网站，只需点击此处添加域名。但由于我使用免费版，每次只能添加一个域名，非常简单。只需按照指引步骤操作，可能从Namecheap或Squarespace获取，或GoDaddy等平台。只需输入那些平台提供的自定义记录，将他们提供的记录添加到这里。

在此创建新的API密钥，称为API密钥。点击创建新API密钥，为其命名，然后添加。确保你的密钥令牌安全并复制后存入环境变量。但最后，你可以看到Resend的免费版每月提供三千封邮件，所以我认为这应该足够大多数初创公司开始了。之后请记得将我们刚设置的最新API密钥复制粘贴到本地环境。

### 4. 设置环境变量与启用pg_net

我们还需要另一个名为内部API密钥的，这是用于Supabase的。这里我们也准备了dotenv的示例，如果你点击进去，需要在底部粘贴你的API密钥，然后将此文件转换为.env.local。在这个教程中我们将使用Sean Chan IO的邮件服务，但你可以随时通过修改代码替换它。如果你进入代码编辑器，你会发现这个邮箱已被我硬编码在名为services/emailService.ts的文件中，你只需将其替换为你自己的邮箱。

下一步我们需要启用pg_net，这是一个帮助你向边缘函数发送HTTP请求的工具。你需要前往SQL编辑器，点击+创建新片段，然后回到你的代码。我们已将所有运行Supabase所需内容放在这个文件夹下，名为Supabase脚本设置和步骤一。好的，你需要做的就是按Command复制，返回Supabase，粘贴到这里并按Command Enter。好的，现在你已获取最新pg_net。下一步需要创建用户邮件日志表，记得我们有一个记录用户接收邮件的表，这非常重要，需要设置锁机制。

### 5. 创建并部署边缘函数

第四步比较特殊，现在是时候介绍边缘函数了。在侧边栏导航到边缘函数，可以看到这里有三个边缘函数：第一个叫发送邮件，第二个发送取消邮件，第三个发送欢迎邮件。如果我点击其中一个，可以看到这些函数的完整仪表盘，我将展示如何设置。所以可以看到这里的调用记录，已发送两封邮件，可以看到执行日志，这里可以看到代码。同样，在详情页也有。请记得取消勾选此框以验证JWT使用旧密钥，否则会阻止你在监督文件夹中运行程序。

我们有一个名为functions的目录，这里有三个index.ts文件。第一个是发送账单邮件，这基本上就是我们要实现的整个功能。我们即将添加两个边缘函数，好的，然后发送欢迎邮件。所以记得替换这个URL为你的实际产品URL，这样不会混淆应用与我的应用。运行方式是复制这段内容到终端并执行，这一步是在安装Supabase CLI，然后登录，按照步骤操作。然后需要替换为你的项目引用，这个URL就在这里，复制粘贴进去。然后需要设置Supabase的密钥：应用URL、Realtime API密钥、内部API密钥。好的，第一个应用URL就是你的应用地址，接下来两个是最近的API接口。

但最后要部署Supabase函数，部署这三个index.ts文件。我刚才演示的就是为了验证这个。你需要前往Supabase仪表盘，查看边缘函数，然后它们应该在这里，并且已激活。你可以点击任一函数编辑代码，然后在这里修改内容。最后，我们还有两步：其中一个叫创建公共用户触发器，另一个叫创建订单取消触发器。我现在会解释它们的作用。我们已经在Supabase定义了边缘函数，这些边缘函数通过Realtime API发送邮件至最终用户，但需要有人触发这个操作。这就是为什么需要使用Supabase触发器函数。触发器实际上存在于你的数据库中，当数据库发生变化时，例如当有人注册或完成支付后，通过Stripe支付并收到API调用和Webhook返回的数据，这些触发器会立即激活并调用所需函数。

### 6. 创建数据库触发器

好的，现在让我们看看。首先创建公共用户触发器，它会在公共用户表创建欢迎邮件触发器，当新用户注册时调用边缘函数。好的，那我们就深入探讨一下。首先我们创建一个名为发送注册欢迎邮件的函数。

请记得将super base项目ref替换为你的实际super base项目ref，它将使用名为发送欢迎邮件的函数。这个函数会在注册时发送欢迎邮件，它由你稍后创建的触发器触发。好的，这个触发器的作用是从信息模式中获取数据，每当有新用户事件发生时，我们将使用触发器执行上面定义的函数，也就是发送欢迎邮件。好的，顺便说一下，这个欢迎邮件是我们定义的边缘函数。

接下来你要做的就是按住command c，所有内容记得替换你的base项目ref，在这里用command v粘贴实际的，替换项目ref，然后点击运行。好的，完成后可以进入数据库，在触发器中可以看到我们有三个触发器，刚才提到的欢迎触发器（文件三）链接到名为发送欢迎邮件的函数。好的，当有人完成注册后，他们的用户ID将从认证表添加到用户表，这个触发器会唤醒并调用名为发送欢迎邮件的函数。

如果我点击这里，可以看到它们位于函数列表中，然后我们也可以查看这个函数的具体内容。好的，编辑函数使用net http post，因为我们最初启用了pg_underscore_net，因此可以进行http调用。好的，然后它将在你的项目中运行，请记住务必替换这部分内容，你将运行发送欢迎邮件的边缘函数，好的，将邮件发送给刚注册的用户，可以看到之前在数据库中查看的函数。

### 7. 部署、测试与总结

我想这就是全部内容了，你可以部署你的应用，这一切都在super base上完成。如果你已有运行中的sales base和stripe应用，就可以直接运行。如果没有stripe，不用担心，你可以直接出发，先用欢迎邮件测试一下。之后可以回到表格编辑器查看用户_邮件，通过_日志查看邮件是否已发送。如果尚未发送，你可以返回重新发送并在此处查看日志。

好的，通常会给出错误提示，如果有错误也没关系，希望这对你们有帮助。我们在自己的初创公司中已经实施了自动IO，这是一个AI销售定制商品和服务的经理，也可以免费试用。我们希望所有人都能顺利引导用户并提供，这是一个非常令人满意的上手体验。如果你觉得这个视频有用，请在GitHub这里给我们一个星标或简单点赞，我们非常感谢你的支持。我很乐意在下方回答任何问题，下次再见，谢谢。好的