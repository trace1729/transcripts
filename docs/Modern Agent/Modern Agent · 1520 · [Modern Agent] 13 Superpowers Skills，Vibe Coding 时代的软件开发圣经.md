# Modern Agent · 1520 · [Modern Agent] 13 Superpowers Skills，Vibe Coding 时代的软件开发圣经
[视频链接](https://www.bilibili.com/video/BV1xswKz7EDh)

## 总结

- 传统保证代码质量的方法如TDD、单元测试、code review和静态检查工具，在Vibe Coding时代面临挑战，而superpowers正是为应对这些挑战而生的。
- superpowers是一个超过7万star的skills集合，通过指令和描述而非代码逻辑来约束AI行为，系统化克服大模型的幻觉、懒惰和讨好倾向。
- skills通过渐进式加载注入上下文，由总控skill 'using superpowers' 在对话开始时触发，定义头脑风暴、写plan、开发（TDD）和验证的完整流程。
- 头脑风暴和写作计划skill将模糊想法转为详细执行方案；开发阶段强调TDD红绿重构、sub agent驱动任务解耦，以及针对高风险任务的执行规划。
- 验证与代码审查是最后防线：必须真实运行验证命令，审查需前置且遵循SOLID、YAGNI等原则，AI审查时验证优先、有理有据反驳、消除模糊性、行动胜于雄辩。

## 大纲

1. Vibe Coding时代传统代码质量保证方法
2. superpowers：以硬编码规范约束AI的skills集合
3. skills的加载机制：渐进式注入与安装方式
4. 总控skill 'using superpowers'的四步流程
5. 头脑风暴与写作计划：从想法到执行方案
6. 开发技能：TDD、sub agent驱动与执行规划
7. 系统调试四阶段法与验证前置原则
8. AI代码审查行为准则与架构设计原则
9. 代码复用与模式抽象：平衡DRY与YAGNI
10. 总结：质量保障核心问题与资源获取

## 正文

### 1. Vibe Coding时代传统代码质量保证方法

亲爱的朋友们，大家中午好！今天我们继续回到我们的MODERA金的这个系列。这期我们来回答一个很重要的问题：web coding时代，我们怎么去保证代码的质量，保证代码是对的，保证代码是可维护的。然后我们这一期后面呢，就是以superpowers这样一个非常非常有名的、一组一个系列的skills来回答这样一个问题——我们怎么去驾驭，怎么去harness（很时髦的一个词），怎么驾驭这个web coding的engineering。

简单整理了一下这个web coding的一个光谱，我对比了这三个工具：VS Code、Cursor，以及Codex和Cloud Code。VS Code呢，假如说我们在VS Code里面不使用任何AI工具，它就是一个代码编程的IDE；假如说我们不用任何AI工具，它纯粹是一个代码编辑器。然后另外一种呢是Cursor，它有一个很强的模型叫Tab模型，我们之前在动态里边也介绍过，它是大规模的在线on-policy的强化学习训练，始终在迭代优化它的那个Tab模型，因为他是有真实的用户反馈的——他提示了一个代码补全，用户接受了还是拒绝了，去做他的一个online on-policy的RL训练，他这个Tab模型是非常非常强的。然后另外呢可以对话式地选择代码文件、选择代码片段，去做一个轻量级的、行级别的一个代码开发。而且很重要的一个事情，他有一个Diff的GUI，他改完之后我们可以用这个工具的source control去对比，看一下他改了什么、删了什么、加了什么。然后很自动化的就是Codex和Cloud Code，它是一个纯自然语言编程的，它就是一个代码的CLI，他们不再有一个IDE——你在命令行里面进到你这个工程里边之后，你Codex一下，进去之后你就纯自然语言编程，然后验证即可。大概有三个谱系。

我们这期回答的就是，我们在web coding的时候，在Codex或者Cloud Code里边，回答几个永恒经典的问题。但至于我们要不要使用web coding，这是另外一个话题，我们后边会再找一期专门的视频来介绍这个事情，这是我目前阶段性的理解。我们要回答这个永恒经典的问题：如何保证代码的准确和稳健性。顾名思义，就是你写的代码是对的。我们可以用TDD、测试驱动开发，你可以写单元测试、模块测试、集成测试，然后你还要追求你的分支覆盖率——假如说你代码里面写了100个if else，你要保证所有的if、所有的else都要走过去，而且都是对的，就所谓的分支覆盖率。所谓的测试，一定要写大量测试。

第二呢，要保证代码的可恢复性，你的代码是clean的，是具有一个很好的设计的。经典的方法就是code review，你要周期性地重构。当然还有一个强制性的约束，你可以用hook——假如你的代码是通过Git管理的，你可以配置一个pre-commit的配置文件，比如要求这个代码的风格。我就以这个Ruff为例哈，它是可以要求你这个函数里边：第一，函数语句不要过长；第二，你的分支不宜过多。他是一个硬性的约束，他是一个静态的检查。大概我们之前的方式是这些，也就是说我们用强制性的流程来约束，保证模型的质量。

### 2. superpowers：以硬编码规范约束AI的skills集合

然后这期我们介绍的是，在web coding时代，似乎好多事情交给这个模型来解决了，但是依然我们可以用superpowers这样一个非常非常powerful的——我们可以看下它的star，现在已经7万多了，而且每天也都在进行大量的更新。我们用这样一个skills，这期我们重点来介绍这个skills的开发主线。

我们来看一下他是怎么去定义这个软件开发的一个流程，怎么通过流程来保证代码的质量，也就是说我们要驾驭这种your harness engineering，这个web coding时代的engineering。好，我们这一期呢，很快就进入到对这个super pass这个介绍哈，因为我们上一期我也讲了哈，codex里面怎么做提示词注入哈，当时也大概回答了一个问题哈，这个skills是什么时候暴露的，是什么时候加载到这个模型上下文里面的，然后就整体的去串讲一下这个superpose里面各种各样的个skill哈。

就是所谓的慢，其实快哈，就是你可以不加任何skill哈，不加任何的提示词哈，不加任何的约束，你有一个需求模型去进行开发，它可能很快写出的代码有可能是不准，也有可能是不可维护的，但是我我们有了这样的约束之后，我们可以保证代码的质量，保证代码它是准确的，同时它是面向未来的，就是我们是可以迭代，可以update，可以不断的去完善哈，而且这个完善呢是效率更高的，也就是所所谓的慢，其实快哈。

这个项目呢不仅仅是一组prompt，它是将一套顶级软件工程实践TDD哈，code review系统化调试啊，这是用的比较多的哈，这几个工具硬编码为这个AI coding时代的规范的一个操作系统。就是因为我是坦白讲，我也毕业毕业工作多年哈，就是当我看到这个skills的时候，我是比较比较惊讶的哈，我是心有戚戚的哈，就他一定是一个资深的一个软件开发的，一个纯运人员，他沉淀下来的比较凝练的一套软件开发的，一个流程的一个描述。

这个硬编码呢它是通过skill哈，通过指令哈，通过描述哈，他不是那么if else的那种强的逻辑的一个约束哈，他不是pre commit那那些hook，它通过cursor或者cloud code或者是codex，他都可以用to或者SK的机制强制A进子，克服大模型常见的幻觉，懒惰和讨好的倾向，尤其是code review的时候哈，很容易讨好哈，他严格遵循经过验证的工程思维链路。

他只字里行间哈，我们读特那个skill的那个MD文件的时候，就是字里行间充斥着对软件工程思想的一个理解和应用哈，就是他是AI协助中的软件工程实践的一个集大成者。所谓的harness engineering就是约束和控制或者叫驾驭哈，因为他是马驹嘛，就马的一个缰绳，我们怎么怎么来驾驭这个越来越强的这个coding Engines，写出来又又对又好又准的这个代码。

### 3. skills的加载机制：渐进式注入与安装方式

好，我们大概我们也要回答几个问题哈，就是skills的渐进式加载哈，另外呢就是我们怎么去理解这里面的skills，我们通过画时序图，流程图的方式，第三个问题呢，我们就是以这个SUPERFAST为参考，如何writing一个好的skills，或者就是什么是好的skills。我大概整理概括了一下哈，就是非常考验你的专家经验，就是你要足够的抽象，而且你因为你这个skills可可复用的，就你那些东西是一个common的，是一个principal的，而且还比较考验你的writing的能力哈，但是writing呢，你可以让这个语言模型来帮你润色，而且我们这边会我们会看到哈，是一个非常formal的一个逻辑性的一个描述。

如果有时间的话，我可以解读一下KIMI的这个PPT skills，他这个skills呢也比较长，我们大概可以用时序图的方式去描述，去刻画他整个SOP他整个工作的一个过程，他怎么去限制模型，按你要求的这个sop去执行。然后就是说这个codex里面，或者任何的cloud code或者coding agent里面，这个skills是什么暴露和加载的呢，我们就以这个codex为例哈，就是我们用户哈，我们进到这个CLR里边，我们发出一个真实的用户请求，他在之前哈，他已经植入了大概四个system prompts，具体来说就第一个是系统级的一个提示。具体来说就第一个是系统级的一个提示。

系统级的一个约束好，然后develop一个消息，就是说你的权限和你个沙箱的一个规则。然后很重要的就是这个这个第三条，这个他甚至帮你去inject了一个，植入了一个user的一个消息，它里边把你这个agency等MD放进去，同时呢把所有的skills的描述给放进去，这是第一次暴露。然后还有一条消息哈，就develop一个消息，就是定义这个协作的一个模式哈，如果你用用了plan哈，他就会走那个plan的那个协作的模式哈，默认的话应该是一个A进程的一个模式哈。

然后就进入到我们用户发一个第一个请求，然后开始经过一个AYSIC的一个多轮的一个交互，在内部哈，这个模型内部完成哈，他不断的去调工具哈，然后执行工具，然后获得工具的返回，再进行下一轮的assistant的一个reining，执行工具哈，这循环这样一个过程哈，直到assistance不调用任何的工具，他完成了最终的回答，完成了代码的修改。好我们把这个定义为一个turn哈。然后在这里哈就加上用户的真实的需求，匹配到了一个具体的skill哈，assistance会调一个工具哈，调一个工具，具体的工具就是execute commands，就是用cat这个命令哈，去把这个具体的EXCEE加载进来，然后这个skill呢就进入到了这个context里边，模型呢基于这个skill的一个描述，去产生后续的所有的交互哈。

这个turn结束之后，就开启新一轮的一个query哈，用户继续迭代他这个需求。也就是说它是通过一种prompt injection的方式哈，在前期的第三条user message里面暴露skill哈，就刚讲了哈，这个第三条消息呢，他第一是放这个AH的MD哈，第二是放这个skills，就是这个A近似点MD。它有两种哈，第一种是整个系统级别的哈，你就在你的home目录底下这个点儿codex文件里边，这个A进点MD，如果你有的话，它就会放进去。另外就是如果假如你在一个code base里面去打开的这个codex的话，他会不会去读你这code base里边这个目录底下，根目录底下这个A进四点MD，他就会放到这个消息里边。

同时呢他在这个instruction里边，他有这个对skills的一个介绍，得整体先先介绍一下，excuse是什么，目前这个环境里边有哪些可用的skills，然后如何使用这个功，JESCAPE啊。我们先在这个第三条user消息里边，去暴露所有可用的skills，然后这个coding ag键词呢基于用户的query哈，去匹配一个最最合适的一个scale，然后去调这个function哈，调这个cat这个命这个这个shell命令哈，去把这个绝对路径下的这个skill点MD给加载进来，然后就得到了一个function cord的一个output，然后放到这个整个系统的coding A键词的一个context里面。

codex里面怎么去安装这个skills的就就比较简单了哈，因为现在是3月11号啊，就是现在这个版本的这个superpose呢，大家可以用自然源性安装哈，我就不展开讲了哈，大概一句话啊啊我看一下哈，呃clad code他有这个用PLUIN的方式去安装，然后codex的话用自然语言的方式让他去操作哈，他就安装好了。而且现在这个版本哈，3月11号这个版本的它是是自主发现的，就是你斜杠skills，你是可以list的这些呃，superpose里面定义的一组skills哦，这就不讲了哈。

然后你也可以更新哈啊，就是你进到这个SUPERHOUSE里面去，good po哈，他就动态的就就下次开启一个新的规划的时候，它就加载进来了哈，他是因为他毕竟是一个本地的一个文本文件哈。然后他现在是14个skill哈，就是每个skill都是通过skill点MD的一个前面的那个格式化的描述哈，去定义哈，什么时候去用哈，use swing哈。

我就不展开读了哈，大家去把这个代码下下来之后，大家可以去读每个具体的skill点。Md，他这个描述里面都是什么时候去用哈。然后using supervise呢，首先有一个作为总控的一个skill，就是use when starting any conversation，就是你在这个codex里面去开启第一轮对话的时候，他就会尝试去用这个superpowers这样一个skill哈，总控的一个skill。它里边定义了我待会具体来讲吧，就是大概的一个链路哈。

### 4. 总控skill 'using superpowers'的四步流程

首先头脑风暴，用户有一个具体的一个query哈，然后去写这个plan。同道former里面会给用户做一个多轮的一个对话，给你确认这个我们这次修改的一个边界，然后去写这个规划，然后去做开发。这个开发的时候，它很重要的一个事情就是TTD测试驱动开发，先写好测试文件，然后针对测试文件去编写具体的代码，然后完成，然后在完成之前要进行验证哈。

好我们来看第一个skill哈，Using superpowers。那这里边我们其实也可以看到哈，就是我先讲两点哈，第一呢大家可以看到哈，他这个我就不展开去介绍他全部的这个sq点MD了哈，大家有兴趣的话也可以去读哈。他这个skill点MD里面有这样一个很强的一句话哈，他是xml tag里面包裹起来的，就是极度的重要，如果你认为那有1%的机会可以使用这个skill哈，一定要触发某个skill。

它这个里面甚至还定义了一个OMMATE的一种dot语言定义的这样一种流程，他是用这种逻辑语言哈，就是用语言模型他比较擅长的一种代码语言去消化理解这个过程。我们把这个可视化出来之后，我们看一下哈，就首先用户的一个消息，同时呢，他要去判断是不是一个plan模式，就如果是plan模式的话，他是否已经经历过头脑风暴了，就如果没有的话，我们要去调用这个头脑风暴这个skill。

有了用户的一个消息，有了一个头脑风暴形成的一个plan哈，那我们就开始去调这个skill to，他还要宣称哈，也就也是强化他对这个skill的应用哈，他要在这个多轮对话里面宣称他在使用什么skill哈，去出于什么样的目的。这个skills里边还有一个好的事情，就是我们要定义一组checklist，我们待会来展开讲。

对大概完整的这样一个流程是在这个using supervisor里面来定义的，它是通过dot语言这种形式化的代码语言来约束代码的行为，而且他这里边有一些巧思哈，就是说他还要宣称让这个coding引擎的宣称自己在使用某种skill去出于什么样的目的。skills里面它也可以定义一组checklist，就逐个的去检查是否完成这个要求。

### 5. 头脑风暴与写作计划：从想法到执行方案

好，那下面就第一个哈，就所谓的这个头脑风暴的这个skill。他是要问一些确认性的问题，一次的我只问一个，要理解purpose，真实的意图约束以及成功的一个准则。他必须先探索上下文逐个提问，给两到三个方案分段的评审，是个机械设计文档，再移交writing plans。

对，他会在这个code base里边，这个DOS plans底下，去建一个当前日期的某一个主题的一个design的一个markdown文件，先形成并保存一个设计的一个doc，然后交给riding plans，形成一个真正的一个实现的一个执行的一个plan。在头脑风暴阶段，他不要调任何的实现那个技巧，不写任何的代码，脚手架大概有这样一个强制的一个约束。

这段语法呢也是这个skill点MD提供的哈，我们看看一下哈，首先先天探索这个项目的上下文，去问一些确认性的问题，跟用户去交流沟通，去明确问题，然后提出两到三个解决方案，然后呈现这个社区的这个section哈，就如果用户批准了这个计划，然后就开始写这个计划，然后最后去调writing plan这样一个skill哈。那下面我们进入到writing plan哈。

这是我对他的那个整个skill点MD的一个提炼哈，就把设计变成一个零上下文工程师可执行的一系列图计划。这里面已经写得非常非常完备了哈，他不需要再去读代码，他就基于这个文档哈就可以做开发了。那这里面有有几个准则哈，DRY就不要重复你自己，也就是避免写重复的代码、重复的逻辑。还有一个YAGNI就不做可能以后用得上的功能或者抽象。然后第三个就是测测试驱动开发，先写测试再写实现，然后频繁的提交小步迭代，保持提交的一个力度小且清晰。这就是他原原文哈，就DRY，然后这些比较经典的软件开发的一个范式哈。

头脑风暴呢产生的是架构设计文档，它解决的是我们要建什么、我们为什么这样建，what and why的问题。何时使用？在任何创造性工作，新功能新组件，修改现有行为的，最开始使用需求往往是只有一个模糊的想法，比如我想加一个用户认证功能。核心的目标就是探索意图和约束，purpose就是用户真正的意图是什么，通过一问一答的方式，弄清楚用户到底想要什么性能要求是什么，有什么限制条件。

提出两到三种不同的解决方案，这也是头脑风暴的一个应有之义嘛，就你要发散，你要给出来多种解决方案，分析各自的trade off，然后用户确认到底用哪个哪个解决方案。它的产出物是一个design的一个markdown的一个文档，它侧重于宏观架构，数据的流向，组建了一个交互错误的异常处理，就里面几乎没有具体的代码实现，更多的是概念和决策。它的终点一旦设计成文档被用户批准并保存，brainstorming技能的最后一步就是强制调用writing plan的一个技能。

writing plan呢写出的是一个实施的一个计划文档，解决的是如何实现的一个问题。在需求和架构设计已经完全明确后，它接在这个brainstorming之完成之后，在真正动手写代码之前，就是他的核心目标是任务拆解，将宏观的设计转化为极细力度可执行的步骤，消除执行时的上上下文依赖。假设最终写代码的工程师或者另外一个sub agent则对项目一无所知，因为我有我，我们有可能哈，我们拿到这样一个实现了一个文档之后，我们另开一个会话去让同样的让codex或者是让让client code去去执行，因此这个计划呢必须极其详尽。

他的产出物是plan点MD，它侧重于微观执行里面包含极其具体的细节，要创建哪个具体确切路径的文件，要修改哪几行，要写什么样的失败测试，要运行什么确切的命令来验证。重点就是计划写完之后不写代码，而是询问用户选择哪种执行方式，是当前会话用撒微镜的去执行，还是开一个新的绘画批量的执行。这就是说的哈，他必须是一个他交付给另外一个工程师，就不需要再重新探索整个项目。

### 6. 开发技能：TDD、sub agent驱动与执行规划

好非常非常经典的TDD哈，TDD里面具体就是红绿重构三个阶段的一个测试驱动的开发的一个纪律。严禁哈，在没有看到测试失败的情况下编写生产代码，因为TDD的第一步哈就写一个会失败的测试。就假如我们要实现一个功能，这个功能是没有的，假如假如我我我给这个功能给它起了一个函数，我们就在这个测试里边去掉这个功能，那自然因为你没有这个实现哈，他一定会报错啊，这是一个红。第一阶段还是先写一个会失败的测试，然后绿写最少量的代码都能通过，先把它pass，然后重构，在保持绿色的前提下优化代码结构，这是一个典型的红绿重构的一个测试驱动开发的一个流程。

也就是说必须亲眼看着测试失败，就是红亮红灯，写完测试后必须运行并看预期的报错，如果测试直接通过了，说明他测的是错的，或者是测的是已经存在的行为。事后测试证明不了任何事情，写完代码再补测试哈，往往只会测试代码现在的行为，而不是代码应该的行为，这称之为技术债务哈。大家一定要注意这个这个观念啊，所谓的测试驱动开发就是先写测试再写开发，而不是你先做开发再做测试。就组装前必须要有证据，就是加你，你宣称你已完成或者测试通过前，你必须真实的运行命令工具并检查，也就是说你要在这个测试里面去写大量的断言哈。

你有对结果的一个预期，那下面就是另外一个就是执行规划，或者sub agent driven的一个development。sub agent driven的一个development，就是计划中的任务彼此独立解低耦合，希望系统自动跑完，为每个任务派发新的sub agent，完成后进行双重的一个机，机器的一个审查规范和质量。然后然后另外一个skill呢执行规划，它是一种宏观层面的一种管理模式哈，他强调的是批处理和人类审查，就是它会定义一些检查点，就是它适用的情况是任务极其复杂，高风险，需要人类步步把关哈，在中间过程里边可能需要人的参与。

那下面就是还有一些具体的哈，就是比如使用这个worktree哈，分发这个parallel agents词，以及另外一对比这个sub agents driven的development，就是有些时候我们需要一个work tree哈，去隔离目前的一个分支，在单个计划执行里，强调每个任务按实现哈规范的一个review，代码质量的顺序去做development。分发呢这并顾名思义哈，并发的话就是多个独立问题并发调查或者修复。

然后另外就是subway，agent模式和parallel模式哈，sub agent的话，就他这完成是在当前规划里面完成，当前绘画有一个plan哈，coding一定则在这个规划里边，按照计划逐步实施额任务，实现跑测试，逐步看结果。另外一种parallel session呢，就用户可以单新开一个会话，按照你的一个就是个实现的plan哈，用这个executing plan哈，去流程批量去推进哈。

好我们在代码开发里面很重要的一个事情，就是系统级别的一个debug，就是很多时候就假如说在装了个代码的时候，发现有不符合预期的或者有问题，我们就会把这个问题抛出来，然后这个时候呢会触发这个superpowers的一个，系统debug in的一个skill哈，就遇到bug或者测试失败时使用，就禁止AI盲目猜测或者试戳时的修复，他强制要求分为四步，第一根因分析就找根因，第二分析模式，然后提出假设，然后最小化实现和验证。

出现了任何bug或者失败，先做根音的调查，禁止拍脑袋决策，他是四阶段的，先更新调查，然后模式分析，然后提出假设实施，如果这个四阶段哈循环了三次，发现还是解决不了问题，说明他架构可能有问题哈，对这个是平常可能也是很高频，容易使用的一个触发的一个skill哈，很多时候我就问为什么我报错了哈，为什么有问题好。

### 7. 系统调试四阶段法与验证前置原则

然后还有一个skill哈，要在完成之前就做验证，就AI喜欢假装完成或者过度自信的一个坏习惯，就是AI宣布任务完成或者测试通过，或者是准备提交前，必须真实的运行验证的命令，并根据实际的输出来宣称已完成了本次开发。然后下面就是很重要的一个事情，就是code review，他有两个哈skill哈，一个是请求做code review，另外是接收code recode review，我们来看哈，就这个review呢一定是前置的，软件工程质量保证的一个重要的理念，就是你要前置的去做review化，而不是累积了很多个update之后再一次性的做code review，就是在软件开发的生命周期的早期阶段，就引入测试和审查，而不是等到开发后期，这样可以以最低的成本发现和修复缺陷。

Review，Early review often，不仅是在合并组分支前需要审查，在每一个极细力度的子任务task完成后，也必须强制触发审查，就是防止你的设计的一个错误，污染下一个任务哈。优秀的code review必须基于清晰的上下文，如果没有需求作为参考，大家也可以看到哈，这个code review的一个输入是什么，第一我们刚刚实现了什么，用户原始的需求或者计划是什么，然后当然还有你这个get了一个地方，我们去查基础的一个get了一个版本，然后当前的一个版本，那这样的话我们就好做def了。

这是code review的一个上下文，那这里边code review的时候，它定义了一个checklist，就是我们最开始讲的，这个skills里面是否有checklist，我们要去逐个的去回归。我们看哈它有哪些checklist，第一代码质量，他是不是关注点分离的，他是不是解耦的，他错误的处理哈，他有没有重复的代码，它编辑的一个情况，在那个写测试用例的时候，有没有cover1些边界的一个corner case，然后架构设计的角色是否合理，可扩展性能的影响，安全的隐患，然后测试他是否撤了真实的逻辑，而不仅仅是Mark就所谓的reward hiking哈，他写一个永远会通过的一个词儿，用力哈边界覆盖率和集成测试，还有这个requirements，它是否满足了计划的所有要求，没有蔓延覆盖，然后他是不是一个生产级别的一个一个完成好。他也定义了这个红行为的红红线哈，就不要严禁不看代码，就说看起来不错，严禁把吹毛求疵标为致命的错误，要避免，要严禁含糊其辞，必须给出最终的结论哈。

好那下面就进入到这个code review哈，Code review，本质呢是一场基于客观代码的一个技术评估，而不是为了讨好review的一个社交表演。要追求一个技术的严谨性，决策必须基于事实测试结果，系统架构的现状，而不是基于直觉，权威或社交压力，也不要做表演性的同意和盲从。有的时候，开发者或者AR为了显得配合或者态度好，在没有验证技术可行性的情况下，盲目统一并执行REVER的建议。还有一个极限编程的原则，如果review建议添加一个看起来很专业，但当前系统根本没有调用的一个功能，必须用这个原则，就你暂时你目前不需要它，就避免过度开发，过度设计好。

### 8. AI代码审查行为准则与架构设计原则

还有一个向后的兼容性哈，我就不展开讲了哈，就验证要先于执行，就收到反馈后严禁说出啊，你绝对对哈，第一步必须是去代码库中去做验证，就reviewer哈，无论是人类还是外部工具，往往缺乏全局的上下文，他们可能建议删除一段看起来无用的代码，但这部分代码可能是为了处理某个特定的相互兼容性，盲目听从会导致系统崩溃。好还要做有理有据的技术的一个反驳，Push back，就如果发现revert1建议是错误的，必须使用技术推理进行反驳，优秀的工程师不仅要会写代码，还要能捍卫系统的架构完整性。反驳不是为了争吵，而是为了保护代码库。如果review要求加一个复杂的指标导出功能，AI应该先搜索代码库，发现没有用到，然后回复，这个端点没有被用到，是否要移除，考虑到这样一种开发的一种范式的话。

还有就是要消除模糊性，如果reverse提了六点意见，其中两点描述不清，严禁先做，懂得两点，把不懂的两点留到最后，严禁这样哈，必须立刻停止，先询问清楚所有细节。就实践的意义在于什么呢，就是软件模块之间往往是高度耦合的，你部分的理解会导致错误的实验方向，返工成本非常非常高。还有一个很重要的，就非常非常资深的一个程序员，会会写的这样一句话，就行动胜于雄辩，就当REVER提的意见是正确的时候，严禁使用任何感激或者讲赞美的是废话哈，正确的做法是直接修复它，并简短陈述事实。高效的工程文化中，代码本身就是最好的回应，消除情绪化的社交废话，可以极大的提高沟通的信噪比。

好，code review里面，它定义了一些架构的设计模式和代码的风格，就比如这个所谓的solids和关注点分离，solid是面向对象设计的五大五大基石哈，我就简单简单讲一下哈，因为我不是特别特别专业哈，这个可能是非常非常资深的一个一个架构师，特别特别心领神会哈。第一是单一责任原则，就避免一个类哈或者一个一个文件，他承担了所有的事情，一定要解耦。第二开闭原则，你这个实现是要对扩展开放，对修改关闭我们可以加功能，但是我们不要改已有的功能。第三呢就是李李四替换原则，这个我就不展开讲了哈，接口隔离原则依赖到倒置原则，尤其适用于就大型的面向对象的这个系统架构的设设计里边的一个4G模式哈。

关注点分离是一种宏观的架构哲学，主张将软件划分为具有不同职责的独立模块，做一个逻辑上的拆分，使得每个模块只解决一个特定的关注点。

Code review 也有自己的检查清单，就刚讲的那几个事情有一些具体的细节，比如是否进行了适当的错误处理，是否保证了类型的安全，corner case，还有魔法数字。这个魔法数字的话，其实我们在 pre commit hook 里面也能保证。而且还要消除防御性编程的这种冗余。在编写代码和脚本时，要求解决问题而不是把问题抛出，要求错误处理必须是明确且有帮助的，而不是简单地吞掉异常。

### 9. 代码复用与模式抽象：平衡DRY与YAGNI

还有关于代码复用，我之前用 Web Co 令的话，很容易发现，因为它要限制它的 context window，避免一次问题的修复就把自己的 context moon 给打爆，所以它就经常只看了部分代码，就会导致它写的有 TUS。很多时候它在多次修改之后出现了代码的冗余，重复的代码不断地出现。

我们要怎么去缓解这个代码复用的问题呢？就在 reading plans 这个地方，和代码审查里面都说了，DRY，Don't repeat yourself，和 YAGNI。就是既要复用代码，不要自己重复自己，也要坚决杜绝过度设计或者过度抽象。

当然它也有自己的 reading skills，它在 skill.md 50 里面也会 link 其他的具体的 MD，某种意义上也是一个关注点分离，强调将解决问题的过程提炼为可复用的模式。就是我们 reading skills 在写一些可以重复利用的 pattern，而不是一次性的脚本或叙述，这本身就是对知识和逻辑抽象的极高要求。

### 10. 总结：质量保障核心问题与资源获取

以上就是本期的全部内容，希望对大家在 fab coding 时代能更好地去驾驭这些 AI 的工具。一定要脑子里面始终存有几个问题：如何保证你的代码是准确的、是稳健的，而且你的代码是可维护的、是 clean 的，是具有一个良好的设计的，是没有代码冗余的。

好，以上就是本期的全部内容，非常非常感谢大家看到这里。就是我们这个付费的这些系列里边，包括我最近的很多的代码，都是放在我们一个私有的代码仓库里边。大家有代码需求的话，一定要在后台私信我你们具体的 GitHub 账户信息，我把大家邀请到这个项目里边来。